# Phase 2.5A — Biomarker Foundation (Tokens + Schema + Seed) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the non-runtime foundation of Phase 2.5 — design tokens, fonts, ESLint guardrails, 8 Convex tables, 32-marker seed, seeder script, CI guard, dev-only admin scaffold, and a `convex/lib/claude.ts` stub — so Plans 2.5B/C/D have a stable substrate to build on. No user surface, no parse pipeline yet.

**Architecture:** Widen-migrate-narrow schema change (all new tables — no existing data to migrate). Seed data lives in `packages/core/seeds/` as human-readable JSON the clinical advisor will eventually review; a Node seeder script reads it and upserts into Convex via an internal mutation. Two-register typographic system (Clinical Luxe for existing screens + Biomarker Editorial for biomarker surfaces) is enforced by an ESLint `no-restricted-imports` rule in both directions. A temporary playground screen renders a few MarkerCard stubs against the new palette so the founder can visually verify fonts + colors on Expo Go iOS before we build real screens in Plan 2.5D.

**Tech Stack:** Plan 2A foundation (Turborepo + pnpm + Convex) + Convex schema validators + zod for JSON validation + expo-font + ESLint 9 flat-config. No new runtime deps in this plan (react-native-reanimated + SVG helpers land in Plan 2.5D).

**Authoritative sources:**

- `docs/superpowers/specs/2026-04-17-phase-2.5-biomarker-foundation-design.md` (authoritative design spec)
- `docs/decisions/2026-04-17-phase-2.5-scope-boundary.md`
- `docs/decisions/2026-04-17-biomarker-design-register.md`
- `docs/decisions/2026-04-17-pregnancy-safety-guard.md`
- `docs/decisions/2026-04-17-biomarker-values-split.md`
- `docs/DESIGN.md` §two-register note
- `docs/DEFERRED.md` Phase 2.5 prerequisites

**Scope boundary — NOT in Plan 2.5A:**

- `parseLabReport` action, Claude integration, classification logic → **Plan 2.5B**
- `intakeUpload` mutation, rate limits, notifications emission, curation admin UI, backfill job, portal contracts → **Plan 2.5C**
- Mobile biomarker component package, 4 screens, upload flow, founder approval gate → **Plan 2.5D**

---

## File structure

### Created

```
convex/
├── biomarker/
│   └── internal/
│       └── seedRanges.ts             — internal mutation for seeder script
├── lib/
│   └── claude.ts                     — model-ID constants + stub (real wire-up in 2.5B)
├── admin.ts                          — dev-only admin module skeleton
└── __tests__/
    └── biomarker/
        └── seed-validation.test.ts   — CI guard: JSON validates, thresholds ordered

packages/core/
├── src/
│   └── tokens/
│       └── biomarker.ts              — biomarkerPalette + biomarkerFonts
└── seeds/
    ├── biomarker-ranges.json         — 32 markers × ~45 reference-range rows
    ├── unit-conversions.json         — ~15 unit conversion rows
    └── README.md                     — how to edit, clinical-review workflow

scripts/
└── seed-biomarker-ranges.ts          — Node script: reads JSON, validates, upserts

apps/mobile/
├── assets/fonts/
│   ├── InstrumentSerif-Regular.ttf
│   ├── InstrumentSerif-Italic.ttf
│   ├── JetBrainsMono-Regular.ttf
│   └── JetBrainsMono-Medium.ttf
└── app/
    └── design-biomarker-preview.tsx  — TEMPORARY playground (deleted at end of plan)

docs/decisions/
└── 2026-04-18-phase-2.5a-as-built.md — completion retro (written at merge prep)
```

### Modified

```
convex/schema.ts                      — +pregnancyStatus on users + 8 new tables
convex/seed/featureFlags.ts           — +biomarker_parsing_enabled seed entry
packages/core/src/tokens/index.ts     — re-export biomarkerPalette + biomarkerFonts
packages/core/package.json            — ensure "./seeds/*" subpath export exists
apps/mobile/app/_layout.tsx           — load 4 new font files via expo-font
eslint.config.js (root)               — +no-restricted-imports rules, both directions
package.json (root)                   — +"test:seed" script + include in CI test sequence
turbo.json                            — +"test:seed" task wiring
checkpoint.md                         — Phase 2.5A status at merge
```

---

## Task 0: Worktree + branch setup

**Files:**

- Create: worktree at `D:/onlyou2-phase-2.5a` on branch `feature/phase-2.5a-foundation`

- [ ] **Step 1: Create worktree from master**

Run: `cd D:/onlyou2 && git worktree add -b feature/phase-2.5a-foundation D:/onlyou2-phase-2.5a master`
Expected: `Preparing worktree ... HEAD is now at 0bd1deb docs(phase-2.5): ...`

- [ ] **Step 2: Verify worktree + install deps**

Run: `cd D:/onlyou2-phase-2.5a && pnpm install --frozen-lockfile`
Expected: dependencies installed, no version drift vs master.

- [ ] **Step 3: Verify convex dev is runnable (but do not start yet)**

Run: `cd D:/onlyou2-phase-2.5a && npx convex --help`
Expected: convex help output.

- [ ] **Step 4: Empty-marker commit**

```bash
cd D:/onlyou2-phase-2.5a
git commit --allow-empty -m "chore(phase-2.5a): worktree scaffold"
```

---

## Task 1: Extend Convex schema with pregnancyStatus + 8 new tables

**Files:**

- Modify: `convex/schema.ts`

- [ ] **Step 1: Read existing schema to understand shape**

Run: `cat convex/schema.ts`
Expected: existing `users`, `otpAttempts`, `sessions`, `featureFlags` tables + genderValidator + roleValidator.

- [ ] **Step 2: Replace `convex/schema.ts` with the full Phase 2.5 schema**

```ts
// convex/schema.ts
import { defineSchema, defineTable } from "convex/server";
import { v } from "convex/values";

import { ROLES } from "../packages/core/src/enums/roles";

const roleValidator = v.union(...ROLES.map((r) => v.literal(r)));
const genderValidator = v.union(
  v.literal("male"),
  v.literal("female"),
  v.literal("other"),
);

const pregnancyStatusValidator = v.union(
  v.literal("pregnant"),
  v.literal("not_pregnant"),
  v.literal("unknown"),
);

const labReportSourceValidator = v.union(
  v.literal("patient_upload"),
  v.literal("lab_upload"),
  v.literal("nurse_flow"),
);

const labReportStatusValidator = v.union(
  v.literal("uploaded"),
  v.literal("analyzing"),
  v.literal("ready"),
  v.literal("parse_failed"),
  v.literal("rejected"),
);

const patientNameMatchValidator = v.union(
  v.literal("match"),
  v.literal("mismatch"),
  v.literal("unknown"),
);

const mimeTypeValidator = v.union(
  v.literal("application/pdf"),
  v.literal("image/jpeg"),
  v.literal("image/png"),
);

const markerStatusValidator = v.union(
  v.literal("optimal"),
  v.literal("sub_optimal"),
  v.literal("action_required"),
  v.literal("unclassified"),
);

const unclassifiedReasonValidator = v.union(
  v.literal("not_in_reference_db"),
  v.literal("profile_incomplete"),
  v.literal("pregnancy_sensitive"),
  v.literal("qualitative_value"),
  v.literal("unit_conversion_missing"),
);

const valueTypeValidator = v.union(
  v.literal("numeric"),
  v.literal("qualitative"),
);

const refRangeSexValidator = v.union(
  v.literal("male"),
  v.literal("female"),
  v.literal("any"),
);

const curationStatusValidator = v.union(
  v.literal("pending"),
  v.literal("in_review"),
  v.literal("resolved"),
  v.literal("wont_fix"),
);

const labOrderStatusValidator = v.union(
  v.literal("draft"),
  v.literal("sent_to_lab"),
  v.literal("awaiting_results"),
  v.literal("results_uploaded"),
  v.literal("cancelled"),
);

const windowTypeValidator = v.union(v.literal("day"), v.literal("month"));

const notificationKindValidator = v.union(
  v.literal("lab_report_ready"),
  v.literal("lab_report_parse_failed"),
  v.literal("lab_report_updated"),
  v.literal("lab_report_uploaded_for_you"),
);

export default defineSchema({
  // ─── existing tables (with users additive change) ──────────────────
  users: defineTable({
    name: v.optional(v.string()),
    phone: v.optional(v.string()),
    email: v.optional(v.string()),
    role: roleValidator,
    gender: v.optional(genderValidator),
    dob: v.optional(v.string()),
    pincode: v.optional(v.string()),
    city: v.optional(v.string()),
    state: v.optional(v.string()),
    address: v.optional(v.string()),
    phoneVerified: v.boolean(),
    profileComplete: v.boolean(),
    isFixture: v.optional(v.boolean()),
    pregnancyStatus: v.optional(pregnancyStatusValidator),
    createdAt: v.number(),
  })
    .index("by_phone", ["phone"])
    .index("by_email", ["email"])
    .index("by_fixture", ["isFixture"]),

  otpAttempts: defineTable({
    phone: v.string(),
    hashedOtp: v.string(),
    expiresAt: v.number(),
    attempts: v.number(),
    createdAt: v.number(),
  }).index("by_phone", ["phone"]),

  sessions: defineTable({
    userId: v.id("users"),
    token: v.string(),
    expiresAt: v.number(),
    createdAt: v.number(),
  })
    .index("by_token", ["token"])
    .index("by_user", ["userId"]),

  featureFlags: defineTable({
    key: v.string(),
    value: v.boolean(),
    updatedAt: v.number(),
  }).index("by_key", ["key"]),

  // ─── Phase 2.5 new tables ──────────────────────────────────────────
  lab_reports: defineTable({
    userId: v.id("users"),
    source: labReportSourceValidator,
    labOrderId: v.optional(v.id("lab_orders")),
    fileId: v.id("_storage"),
    mimeType: mimeTypeValidator,
    fileSizeBytes: v.number(),
    contentHash: v.string(),
    collectionDate: v.optional(v.string()),
    patientNameOnReport: v.optional(v.string()),
    patientNameMatch: v.optional(patientNameMatchValidator),
    status: labReportStatusValidator,
    errorCode: v.optional(v.string()),
    errorMessage: v.optional(v.string()),
    createdAt: v.number(),
    deletedAt: v.optional(v.number()),
  })
    .index("by_user_created", ["userId", "createdAt"])
    .index("by_user_hash", ["userId", "contentHash"])
    .index("by_lab_order", ["labOrderId"])
    .index("by_status", ["status"])
    .index("by_deleted", ["deletedAt"]),

  biomarker_reports: defineTable({
    labReportId: v.id("lab_reports"),
    userId: v.id("users"),
    collectionDate: v.optional(v.string()),
    narrative: v.string(),
    narrativeModel: v.string(),
    optimalCount: v.number(),
    subOptimalCount: v.number(),
    actionRequiredCount: v.number(),
    unclassifiedCount: v.number(),
    analyzedAt: v.number(),
    lastReclassifiedAt: v.optional(v.number()),
    deletedAt: v.optional(v.number()),
  })
    .index("by_user_analyzed", ["userId", "analyzedAt"])
    .index("by_user_collection_date", ["userId", "collectionDate"])
    .index("by_lab_report", ["labReportId"])
    .index("by_deleted", ["deletedAt"]),

  biomarker_values: defineTable({
    biomarkerReportId: v.id("biomarker_reports"),
    userId: v.id("users"),
    collectionDate: v.optional(v.string()),
    canonicalId: v.optional(v.string()),
    nameOnReport: v.string(),
    qualifier: v.optional(v.string()),
    valueType: valueTypeValidator,
    rawValue: v.string(),
    numericValue: v.optional(v.number()),
    rawUnit: v.optional(v.string()),
    canonicalUnit: v.optional(v.string()),
    convertedValue: v.optional(v.number()),
    labPrintedRange: v.optional(v.string()),
    status: markerStatusValidator,
    unclassifiedReason: v.optional(unclassifiedReasonValidator),
    category: v.optional(v.string()),
    referenceRangeId: v.optional(v.id("biomarker_reference_ranges")),
    pageNumber: v.optional(v.number()),
    confidence: v.optional(v.number()),
    classifiedAt: v.number(),
    deletedAt: v.optional(v.number()),
  })
    .index("by_report", ["biomarkerReportId"])
    .index("by_user_canonical_date", [
      "userId",
      "canonicalId",
      "collectionDate",
    ])
    .index("by_canonical_status", ["canonicalId", "status"])
    .index("by_report_canonical", ["biomarkerReportId", "canonicalId"])
    .index("by_deleted", ["deletedAt"]),

  biomarker_reference_ranges: defineTable({
    canonicalId: v.string(),
    displayName: v.string(),
    aliases: v.array(v.string()),
    category: v.string(),
    canonicalUnit: v.string(),
    ageMin: v.number(),
    ageMax: v.number(),
    sex: refRangeSexValidator,
    pregnancySensitive: v.boolean(),
    optimalMin: v.number(),
    optimalMax: v.number(),
    subOptimalBelowMin: v.optional(v.number()),
    subOptimalAboveMax: v.optional(v.number()),
    actionBelow: v.optional(v.number()),
    actionAbove: v.optional(v.number()),
    explainer: v.string(),
    source: v.string(),
    clinicalReviewer: v.optional(v.string()),
    reviewedAt: v.optional(v.number()),
    isActive: v.boolean(),
    updatedAt: v.number(),
  })
    .index("by_canonical_id", ["canonicalId", "sex", "ageMin"])
    .index("by_active", ["isActive", "updatedAt"]),

  biomarker_curation_queue: defineTable({
    normalizedKey: v.string(),
    nameOnReport: v.string(),
    rawUnit: v.optional(v.string()),
    sampleLabPrintedRange: v.optional(v.string()),
    firstSeenBiomarkerReportId: v.id("biomarker_reports"),
    occurrenceCount: v.number(),
    lastSeenAt: v.number(),
    status: curationStatusValidator,
    resolvedReferenceRangeId: v.optional(v.id("biomarker_reference_ranges")),
    resolvedAt: v.optional(v.number()),
    resolvedByUserId: v.optional(v.id("users")),
  })
    .index("by_normalized_key", ["normalizedKey"])
    .index("by_status_prevalence", ["status", "occurrenceCount"]),

  lab_orders: defineTable({
    userId: v.id("users"),
    orderedByUserId: v.optional(v.id("users")),
    labPartnerId: v.optional(v.string()),
    panelName: v.optional(v.string()),
    requestedMarkers: v.array(v.string()),
    status: labOrderStatusValidator,
    labReportId: v.optional(v.id("lab_reports")),
    createdAt: v.number(),
    updatedAt: v.number(),
  })
    .index("by_user", ["userId", "createdAt"])
    .index("by_lab_partner_status", ["labPartnerId", "status"]),

  parse_rate_limits: defineTable({
    userId: v.id("users"),
    windowType: windowTypeValidator,
    dateBucket: v.string(),
    count: v.number(),
    updatedAt: v.number(),
  })
    .index("by_user_window", ["userId", "windowType", "dateBucket"])
    .index("by_date_bucket", ["dateBucket"]),

  notifications: defineTable({
    userId: v.id("users"),
    kind: notificationKindValidator,
    biomarkerReportId: v.optional(v.id("biomarker_reports")),
    labReportId: v.optional(v.id("lab_reports")),
    title: v.string(),
    body: v.string(),
    readAt: v.optional(v.number()),
    createdAt: v.number(),
    pushSentAt: v.optional(v.number()),
  })
    .index("by_user_created", ["userId", "createdAt"])
    .index("by_user_unread", ["userId", "readAt"]),
});
```

- [ ] **Step 3: Run typecheck**

Run: `pnpm -w typecheck`
Expected: all 7 workspaces green; no TS errors.

- [ ] **Step 4: Push schema to dev Convex deployment**

Run: `npx convex dev --once --typecheck-components`
Expected: schema pushes cleanly, existing data preserved.

- [ ] **Step 5: Commit**

```bash
git add convex/schema.ts
git commit -m "feat(phase-2.5a/schema): add pregnancyStatus + 8 biomarker tables"
```

---

## Task 2: Seed `biomarker_parsing_enabled` feature flag

**Files:**

- Modify: `convex/seed/featureFlags.ts`

- [ ] **Step 1: Read existing flag seed**

Run: `cat convex/seed/featureFlags.ts`
Expected: existing seed invocation that upserts ~7 flags.

- [ ] **Step 2: Add `biomarker_parsing_enabled` to the seed list**

Add the flag to the existing `FLAGS` / seed array so it upserts with `value: true`:

```ts
// inside the existing FLAGS array (adapt to actual structure)
{ key: "biomarker_parsing_enabled", value: true },
```

- [ ] **Step 3: Run the seed locally**

Run: `npx convex run seed:seedFeatureFlags` (or whatever the existing seed invocation is — read `convex/seed/featureFlags.ts` for the exported name).
Expected: idempotent upsert; no errors; `biomarker_parsing_enabled: true` visible in Convex dashboard.

- [ ] **Step 4: Commit**

```bash
git add convex/seed/featureFlags.ts
git commit -m "feat(phase-2.5a/flags): seed biomarker_parsing_enabled kill switch"
```

---

## Task 3: `convex/lib/claude.ts` — model-ID constants + fail-loud stub

**Files:**

- Create: `convex/lib/claude.ts`

- [ ] **Step 1: Write the stub**

```ts
// convex/lib/claude.ts
//
// Centralized Claude API constants and call-site stubs. Phase 2.5A ships
// the constants and fail-loud stubs. Phase 2.5B wires the real Anthropic
// client. Model IDs MUST be verified against current Anthropic docs at
// implementation time in 2.5B (via Context7 / web fetch) — never
// hard-coded from memory or session state. See
// docs/decisions/2026-04-17-claude-vision-native-parsing.md.

export const MODEL_EXTRACTION = "VERIFY_AT_IMPLEMENTATION_PLAN_2_5B";
export const MODEL_NARRATIVE = "VERIFY_AT_IMPLEMENTATION_PLAN_2_5B";

export interface ExtractMarkersInput {
  fileBase64: string;
  mimeType: "application/pdf" | "image/jpeg" | "image/png";
}

export interface ExtractedPayload {
  is_lab_report: boolean;
  patient_name_on_report: string | null;
  collection_date: string | null;
  markers: Array<{
    name_on_report: string;
    canonical_id_guess: string | null;
    qualifier: string | null;
    value_type: "numeric" | "qualitative";
    raw_value: string;
    numeric_value: number | null;
    raw_unit: string | null;
    lab_printed_range: string | null;
    page_number: number;
    confidence: number;
  }>;
}

export interface GenerateNarrativeInput {
  markers: Array<{
    canonicalId: string | null;
    displayName: string;
    status: "optimal" | "sub_optimal" | "action_required" | "unclassified";
    category: string | null;
  }>;
}

export async function extractMarkers(
  _input: ExtractMarkersInput,
): Promise<ExtractedPayload> {
  throw new Error(
    "extractMarkers: not wired yet — ships in Phase 2.5B. See plan 2.5B Task N.",
  );
}

export async function generateNarrative(
  _input: GenerateNarrativeInput,
): Promise<string> {
  throw new Error(
    "generateNarrative: not wired yet — ships in Phase 2.5B. See plan 2.5B Task N.",
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `pnpm -w typecheck`
Expected: green.

- [ ] **Step 3: Commit**

```bash
git add convex/lib/claude.ts
git commit -m "feat(phase-2.5a/claude): add model-ID constants + fail-loud stubs"
```

---

## Task 4: `convex/admin.ts` dev-only scaffold

**Files:**

- Create: `convex/admin.ts`

- [ ] **Step 1: Write the dev-only scaffold**

```ts
// convex/admin.ts
//
// Dev-only administrative operations. Every mutation + action in this file
// MUST runtime-check that the deployment is not production before doing
// anything. Phase 2.5A ships the scaffold only; Plans 2.5B/C add actual
// operations (simulateLabUpload etc.).
//
// Guard pattern:
//   assertNotProd(ctx);
// Throws "admin operations are disabled in production" if the current
// Convex deployment name matches a production pattern.

import type { MutationCtx, ActionCtx } from "./_generated/server";

const PROD_DEPLOYMENT_PATTERNS = [/^prod$/i, /prod$/i, /^production$/i];

export function assertNotProd(_ctx: MutationCtx | ActionCtx): void {
  const deployment = process.env.CONVEX_DEPLOYMENT ?? "";
  const isProd = PROD_DEPLOYMENT_PATTERNS.some((p) => p.test(deployment));
  if (isProd) {
    throw new Error(
      "admin operations are disabled in production (CONVEX_DEPLOYMENT matched a prod pattern)",
    );
  }
}
```

- [ ] **Step 2: Typecheck**

Run: `pnpm -w typecheck`
Expected: green.

- [ ] **Step 3: Commit**

```bash
git add convex/admin.ts
git commit -m "feat(phase-2.5a/admin): dev-only admin module scaffold with prod-hardfail guard"
```

---

## Task 5: Biomarker token module

**Files:**

- Create: `packages/core/src/tokens/biomarker.ts`
- Modify: `packages/core/src/tokens/index.ts`

- [ ] **Step 1: Read existing tokens/index.ts to understand export pattern**

Run: `cat packages/core/src/tokens/index.ts`
Expected: current re-exports for colors / typography / spacing.

- [ ] **Step 2: Create `packages/core/src/tokens/biomarker.ts`**

```ts
// packages/core/src/tokens/biomarker.ts
//
// Biomarker Editorial palette + fonts. Second of two design registers in
// the Onlyou patient app. Must ONLY be imported from biomarker-surface
// paths (see eslint.config.js no-restricted-imports rule).
//
// See docs/decisions/2026-04-17-biomarker-design-register.md.
// Light palette only in Phase 2.5; dark vars documented below but
// intentionally not exported.

export const biomarkerPalette = {
  // Surface
  pageBg: "#EFE5D4",
  bg: "#F6F1E9",
  bg2: "#EFE8DC",
  bg3: "#E7DFD0",
  bg4: "#DDD3C0",

  // Lines + ink
  line: "rgba(40,26,14,0.08)",
  line2: "rgba(40,26,14,0.16)",
  ink: "#1C1612",
  ink2: "#3A2F24",
  muted: "#7A6A57",
  muted2: "#A69581",

  // Accent
  amber: "#B4641F",
  amber2: "#8A4A14",
  honey: "#B57A2F",

  // Status
  sage: "#5C6E4A",
  rose: "#A24636",
  lavender: "#7B6E9C",
  grey: "#9A8F80",
} as const;

export type BiomarkerPaletteKey = keyof typeof biomarkerPalette;

export const biomarkerFonts = {
  display: "InstrumentSerif_400Regular",
  displayItalic: "InstrumentSerif_400Regular_Italic",
  mono: "JetBrainsMono_400Regular",
  monoMed: "JetBrainsMono_500Medium",
  sans: "Inter_400Regular",
  sansMed: "Inter_500Medium",
  sansSemibold: "Inter_600SemiBold",
} as const;

export type BiomarkerFontKey = keyof typeof biomarkerFonts;

// Dark-mode reference values (NOT exported — defined here for future dark
// mode wiring; commented so they do not accidentally land in light mode):
//   pageBg: '#0A0806', bg: '#13100D', bg2: '#1A1612', bg3: '#211C17',
//   bg4: '#2A231C', line: 'rgba(230,210,190,0.08)', ...
// See docs/DEFERRED.md Phase 2.5 section "Dark mode wiring → post-foundation".
```

- [ ] **Step 3: Re-export from the tokens barrel**

Append to `packages/core/src/tokens/index.ts`:

```ts
export { biomarkerPalette, biomarkerFonts } from "./biomarker";
export type { BiomarkerPaletteKey, BiomarkerFontKey } from "./biomarker";
```

- [ ] **Step 4: Typecheck**

Run: `pnpm -w typecheck`
Expected: green.

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/tokens/biomarker.ts packages/core/src/tokens/index.ts
git commit -m "feat(phase-2.5a/tokens): add biomarkerPalette + biomarkerFonts (editorial register)"
```

---

## Task 6: ESLint guardrails for two-register token isolation

**Files:**

- Modify: root `eslint.config.js`

- [ ] **Step 1: Read existing root eslint config**

Run: `cat eslint.config.js`
Expected: existing flat-config with rules for each workspace.

- [ ] **Step 2: Add `no-restricted-imports` rules in both directions**

Inside the rule set that applies to mobile sources, add a rule that blocks `biomarkerPalette` / `biomarkerFonts` outside biomarker-surface paths, and a rule that blocks the Clinical Luxe `colors` token inside biomarker-surface paths.

Add this override block (adapt file pattern syntax to the existing config's override style):

```js
// eslint.config.js — add to the overrides array
{
  files: [
    "apps/mobile/src/**/*.{ts,tsx}",
    "apps/mobile/app/**/*.{ts,tsx}",
  ],
  rules: {
    "no-restricted-imports": [
      "error",
      {
        paths: [
          {
            name: "@onlyou/core/tokens",
            importNames: ["biomarkerPalette", "biomarkerFonts"],
            message:
              "biomarkerPalette/biomarkerFonts are only allowed in biomarker-surface files: apps/mobile/app/lab-results/**, apps/mobile/app/lab-booking/upload-results*, apps/mobile/src/components/biomarker/**. See docs/decisions/2026-04-17-biomarker-design-register.md.",
          },
        ],
      },
    ],
  },
},
// Allow biomarker imports within biomarker-surface paths only
{
  files: [
    "apps/mobile/app/lab-results/**/*.{ts,tsx}",
    "apps/mobile/app/lab-booking/upload-results*.{ts,tsx}",
    "apps/mobile/src/components/biomarker/**/*.{ts,tsx}",
  ],
  rules: {
    "no-restricted-imports": [
      "error",
      {
        paths: [
          {
            name: "@onlyou/core/tokens",
            importNames: ["colors"],
            message:
              "Clinical Luxe `colors` is not allowed in biomarker-surface files; use `biomarkerPalette` instead. See docs/decisions/2026-04-17-biomarker-design-register.md.",
          },
        ],
      },
    ],
  },
},
```

- [ ] **Step 3: Write a deliberate-violation test file (temporary)**

Create `apps/mobile/src/__eslint-probe__/register-violation.ts`:

```ts
// TEMPORARY — delete before commit; proves the eslint rule fires
import { biomarkerPalette } from "@onlyou/core/tokens";
export const x = biomarkerPalette.amber;
```

Run: `pnpm --filter @onlyou/mobile lint`
Expected: error at that file: "biomarkerPalette/biomarkerFonts are only allowed in biomarker-surface files..."

Delete the probe file:

```bash
rm apps/mobile/src/__eslint-probe__/register-violation.ts
rmdir apps/mobile/src/__eslint-probe__
```

Re-run: `pnpm --filter @onlyou/mobile lint`
Expected: clean.

- [ ] **Step 4: Commit**

```bash
git add eslint.config.js
git commit -m "feat(phase-2.5a/tokens): eslint guardrails block cross-register token imports"
```

---

## Task 7: Font TTFs + expo-font loader

**Files:**

- Create: `apps/mobile/assets/fonts/InstrumentSerif-Regular.ttf`
- Create: `apps/mobile/assets/fonts/InstrumentSerif-Italic.ttf`
- Create: `apps/mobile/assets/fonts/JetBrainsMono-Regular.ttf`
- Create: `apps/mobile/assets/fonts/JetBrainsMono-Medium.ttf`
- Modify: `apps/mobile/app/_layout.tsx`

- [ ] **Step 1: Download the TTFs from Google Fonts (OFL)**

The fonts are Instrument Serif (OFL) and JetBrains Mono (OFL). Download from Google Fonts API:

```bash
mkdir -p apps/mobile/assets/fonts
curl -L -o apps/mobile/assets/fonts/InstrumentSerif-Regular.ttf \
  "https://github.com/Instrument/serif/raw/main/fonts/ttf/InstrumentSerif-Regular.ttf"
curl -L -o apps/mobile/assets/fonts/InstrumentSerif-Italic.ttf \
  "https://github.com/Instrument/serif/raw/main/fonts/ttf/InstrumentSerif-Italic.ttf"
curl -L -o apps/mobile/assets/fonts/JetBrainsMono-Regular.ttf \
  "https://github.com/JetBrains/JetBrainsMono/raw/master/fonts/ttf/JetBrainsMono-Regular.ttf"
curl -L -o apps/mobile/assets/fonts/JetBrainsMono-Medium.ttf \
  "https://github.com/JetBrains/JetBrainsMono/raw/master/fonts/ttf/JetBrainsMono-Medium.ttf"
```

If curl fails (repo-layout drift), fall back to fetching from Google Fonts CSS directly — inspect the `src: url(...)` entry at `https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&display=swap` with a browser User-Agent header, then fetch the TTFs from the resulting `fonts.gstatic.com` URLs.

Verify each file is non-empty:

```bash
ls -la apps/mobile/assets/fonts/
```

Expected: four .ttf files, each 10KB–500KB, non-zero.

- [ ] **Step 2: Read existing `apps/mobile/app/_layout.tsx` to see the font-loading pattern**

Run: `cat apps/mobile/app/_layout.tsx`
Expected: existing `useFonts` hook call with Playfair Display variants + Inter variants. Splash-screen hold-until-loaded wiring.

- [ ] **Step 3: Add the 4 new fonts to the `useFonts` map**

Inside the existing `useFonts({...})` call:

```ts
InstrumentSerif_400Regular: require("../assets/fonts/InstrumentSerif-Regular.ttf"),
InstrumentSerif_400Regular_Italic: require("../assets/fonts/InstrumentSerif-Italic.ttf"),
JetBrainsMono_400Regular: require("../assets/fonts/JetBrainsMono-Regular.ttf"),
JetBrainsMono_500Medium: require("../assets/fonts/JetBrainsMono-Medium.ttf"),
```

- [ ] **Step 4: Run the app on Expo Go iOS**

Run: `pnpm --filter @onlyou/mobile start` (or `pnpm mobile:dev`). Scan QR with Expo Go.
Expected: splash screen holds briefly while fonts load, then welcome screen renders normally — no font-missing warnings in Metro logs.

- [ ] **Step 5: Commit**

```bash
git add apps/mobile/assets/fonts/ apps/mobile/app/_layout.tsx
git commit -m "feat(phase-2.5a/fonts): add Instrument Serif + JetBrains Mono fonts"
```

---

## Task 8: Temporary playground screen for visual verification

**Files:**

- Create: `apps/mobile/app/design-biomarker-preview.tsx` (DELETED at end of plan)

- [ ] **Step 1: Write the playground screen**

```tsx
// apps/mobile/app/design-biomarker-preview.tsx
//
// TEMPORARY visual sanity check for Phase 2.5A tokens + fonts.
// DELETED at end of plan in Task 17. Do not import from other screens.
import { ScrollView, Text, View } from "react-native";

import { biomarkerFonts, biomarkerPalette } from "@onlyou/core/tokens";

export default function BiomarkerPreviewScreen() {
  return (
    <ScrollView
      style={{ backgroundColor: biomarkerPalette.pageBg, flex: 1 }}
      contentContainerStyle={{ padding: 24, paddingTop: 80, gap: 24 }}
    >
      <Text
        style={{
          color: biomarkerPalette.ink,
          fontFamily: biomarkerFonts.display,
          fontSize: 44,
          letterSpacing: -0.5,
        }}
      >
        Biomarker Report
      </Text>

      <Text
        style={{
          color: biomarkerPalette.muted,
          fontFamily: biomarkerFonts.mono,
          fontSize: 11,
          letterSpacing: 1.2,
          textTransform: "uppercase",
        }}
      >
        The Clinical Curator
      </Text>

      <Text
        style={{
          color: biomarkerPalette.ink2,
          fontFamily: biomarkerFonts.sans,
          fontSize: 16,
          lineHeight: 24,
        }}
      >
        Overall, your metabolic health is trending in the right direction.
        Vitamin D and ferritin remain below the optimal range — your doctor will
        discuss this with you.
      </Text>

      <View
        style={{
          backgroundColor: biomarkerPalette.bg3,
          borderColor: biomarkerPalette.line2,
          borderRadius: 12,
          borderWidth: 1,
          padding: 16,
        }}
      >
        {[
          { name: "Vitamin D", status: "sub_optimal", value: "22 ng/mL" },
          { name: "LDL Cholesterol", status: "optimal", value: "92 mg/dL" },
          { name: "HS-CRP", status: "unclassified", value: "1.2 mg/L" },
          { name: "TSH", status: "action_required", value: "8.4 µIU/mL" },
        ].map((m) => (
          <View
            key={m.name}
            style={{
              borderBottomColor: biomarkerPalette.line,
              borderBottomWidth: 1,
              flexDirection: "row",
              justifyContent: "space-between",
              paddingVertical: 12,
            }}
          >
            <View>
              <Text
                style={{
                  color: biomarkerPalette.ink,
                  fontFamily: biomarkerFonts.sansSemibold,
                  fontSize: 16,
                }}
              >
                {m.name}
              </Text>
              <Text
                style={{
                  color:
                    m.status === "optimal"
                      ? biomarkerPalette.sage
                      : m.status === "sub_optimal"
                        ? biomarkerPalette.lavender
                        : m.status === "action_required"
                          ? biomarkerPalette.rose
                          : biomarkerPalette.grey,
                  fontFamily: biomarkerFonts.mono,
                  fontSize: 10,
                  letterSpacing: 1,
                  marginTop: 4,
                  textTransform: "uppercase",
                }}
              >
                {m.status.replace("_", " ")}
              </Text>
            </View>
            <Text
              style={{
                color: biomarkerPalette.ink,
                fontFamily: biomarkerFonts.monoMed,
                fontSize: 15,
              }}
            >
              {m.value}
            </Text>
          </View>
        ))}
      </View>
    </ScrollView>
  );
}
```

- [ ] **Step 2: Verify the screen renders on Expo Go iOS**

Navigate to `/design-biomarker-preview` (Expo Router auto-registers from the filename). Verify on device:

- Warm parchment background (`#EFE5D4`).
- Serif "Biomarker Report" heading in Instrument Serif.
- Uppercase mono tagline "THE CLINICAL CURATOR" in JetBrains Mono.
- 4 marker rows with correct status colors: optimal = sage, sub-optimal = lavender, action = rose, unclassified = grey.

Founder visual sanity check here — if fonts or palette feel off, capture feedback before moving on.

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/app/design-biomarker-preview.tsx
git commit -m "chore(phase-2.5a): temporary biomarker token preview screen"
```

---

## Task 9: `packages/core/seeds/biomarker-ranges.json` — 32 markers

**Files:**

- Create: `packages/core/seeds/biomarker-ranges.json`
- Create: `packages/core/seeds/README.md`

**Reference sources** (cite in each row's `source` field):

- Endocrine Society Clinical Practice Guidelines (2024 editions)
- Indian Academy of Pediatrics / IAPE adult reference ranges
- American Association for Clinical Chemistry (AACC) desktop reference
- WHO anaemia thresholds (hemoglobin)
- ADA 2024 standards (HbA1c, glucose, lipids)
- Manufacturer-inserts for Roche / Siemens IVD assays (cross-referenced)

Rows in this seed are **DRAFT**. `clinicalReviewer` is set to `"DRAFT — pending review"` on every row; `reviewedAt` is `null`. The seeder script requires `ALLOW_UNREVIEWED_RANGES=1` to load draft rows in dev and hard-fails in prod regardless of env.

- [ ] **Step 1: Write `packages/core/seeds/README.md`**

```markdown
# Biomarker seed data

Flat JSON files that seed the `biomarker_reference_ranges` and related tables.
Human-readable; the clinical advisor reviews this file directly.

## Files

- `biomarker-ranges.json` — 32 markers × ~45 reference-range rows.
- `unit-conversions.json` — unit conversion factors used during parsing.

## Review workflow

1. PR modifies a row.
2. Clinical advisor reviews the diff (GitHub or markdown export).
3. Advisor sets `clinicalReviewer: "Dr. <Name>"` and
   `reviewedAt: "<ISO timestamp>"` on every reviewed row.
4. PR merges only when every modified row has both fields set.
5. Deploy pipeline runs `scripts/seed-biomarker-ranges.ts`. In prod the
   seeder hard-fails on any row with `clinicalReviewer` empty or set to
   `"DRAFT — pending review"`.

## Editing rules

- **Threshold invariant** (enforced by `seed-validation.test.ts`):
  `actionBelow < subOptimalBelowMin < optimalMin ≤ optimalMax < subOptimalAboveMax < actionAbove`
  for bounds that exist on the row.
- **`pregnancySensitive` is mandatory** on every row.
- **`source` is mandatory** — cite the specific guideline or reference.
- **Aliases are case-preserving** but matched case-insensitive at parse time;
  include common variants Indian labs print (e.g., `Vit D`, `25-OH Vitamin D`,
  `25-hydroxyvitamin D`).
- **Sex-differentiated markers** need a row per sex (or a `sex: "any"` row
  if ranges do not differ).
- **Adding a new marker** is a JSON change + re-run of the seeder script —
  NOT a code change.
```

- [ ] **Step 2: Write `packages/core/seeds/biomarker-ranges.json` — first tranche of 8 markers (full rows)**

Create the file. Include these 8 as-fully-populated exemplars:

```json
[
  {
    "canonicalId": "vitamin_d",
    "displayName": "Vitamin D (25-OH)",
    "aliases": [
      "25-OH Vitamin D",
      "25-hydroxyvitamin D",
      "Vit D (25-OH)",
      "Vitamin D Total",
      "Vit D",
      "25 Hydroxy Vitamin D"
    ],
    "category": "Nutrient Health",
    "canonicalUnit": "ng/mL",
    "ageMin": 18,
    "ageMax": 120,
    "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 30,
    "optimalMax": 100,
    "subOptimalBelowMin": 20,
    "subOptimalAboveMax": 150,
    "actionBelow": 20,
    "actionAbove": 150,
    "explainer": "Vitamin D supports bone health, immune function, and muscle strength. Levels below 30 ng/mL are considered insufficient; below 20 is deficient. Adequate sun exposure and supplementation typically correct low levels within 8–12 weeks.",
    "source": "Endocrine Society Clinical Practice Guideline on Vitamin D (2024 update)",
    "clinicalReviewer": "DRAFT — pending review",
    "reviewedAt": null,
    "isActive": true
  },
  {
    "canonicalId": "vitamin_b12",
    "displayName": "Vitamin B12",
    "aliases": ["B12", "Cobalamin", "Cyanocobalamin", "Vit B12"],
    "category": "Nutrient Health",
    "canonicalUnit": "pg/mL",
    "ageMin": 18,
    "ageMax": 120,
    "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 400,
    "optimalMax": 900,
    "subOptimalBelowMin": 200,
    "subOptimalAboveMax": 2000,
    "actionBelow": 200,
    "actionAbove": 2000,
    "explainer": "Vitamin B12 is essential for red blood cell formation and neurological function. Low levels can cause fatigue, numbness, and anaemia; common in vegetarian/vegan diets and in people with gut absorption issues.",
    "source": "National Institutes of Health — Office of Dietary Supplements (2024)",
    "clinicalReviewer": "DRAFT — pending review",
    "reviewedAt": null,
    "isActive": true
  },
  {
    "canonicalId": "folate_b9",
    "displayName": "Folate (B9)",
    "aliases": ["Folic Acid", "Vitamin B9", "Serum Folate", "B9"],
    "category": "Nutrient Health",
    "canonicalUnit": "ng/mL",
    "ageMin": 18,
    "ageMax": 120,
    "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 5,
    "optimalMax": 20,
    "subOptimalBelowMin": 3,
    "subOptimalAboveMax": 25,
    "actionBelow": 3,
    "actionAbove": 25,
    "explainer": "Folate is required for DNA synthesis and red blood cell production. Adequate levels are important for cardiovascular and neurological health.",
    "source": "Institute of Medicine DRI — Folate (2024 review)",
    "clinicalReviewer": "DRAFT — pending review",
    "reviewedAt": null,
    "isActive": true
  },
  {
    "canonicalId": "ferritin",
    "displayName": "Ferritin",
    "aliases": ["Serum Ferritin", "Fe Ferritin"],
    "category": "Nutrient Health",
    "canonicalUnit": "ng/mL",
    "ageMin": 18,
    "ageMax": 120,
    "sex": "male",
    "pregnancySensitive": true,
    "optimalMin": 50,
    "optimalMax": 300,
    "subOptimalBelowMin": 30,
    "subOptimalAboveMax": 400,
    "actionBelow": 30,
    "actionAbove": 400,
    "explainer": "Ferritin reflects iron stores. Low levels indicate iron deficiency; high levels can indicate inflammation or iron overload.",
    "source": "WHO / AACC ferritin reference (2024)",
    "clinicalReviewer": "DRAFT — pending review",
    "reviewedAt": null,
    "isActive": true
  },
  {
    "canonicalId": "ferritin",
    "displayName": "Ferritin",
    "aliases": ["Serum Ferritin", "Fe Ferritin"],
    "category": "Nutrient Health",
    "canonicalUnit": "ng/mL",
    "ageMin": 18,
    "ageMax": 120,
    "sex": "female",
    "pregnancySensitive": true,
    "optimalMin": 30,
    "optimalMax": 200,
    "subOptimalBelowMin": 15,
    "subOptimalAboveMax": 300,
    "actionBelow": 15,
    "actionAbove": 300,
    "explainer": "Ferritin reflects iron stores. Low levels indicate iron deficiency; high levels can indicate inflammation or iron overload.",
    "source": "WHO / AACC ferritin reference (2024)",
    "clinicalReviewer": "DRAFT — pending review",
    "reviewedAt": null,
    "isActive": true
  },
  {
    "canonicalId": "tsh",
    "displayName": "TSH",
    "aliases": [
      "Thyroid Stimulating Hormone",
      "Thyrotropin",
      "TSH Ultrasensitive"
    ],
    "category": "Thyroid",
    "canonicalUnit": "µIU/mL",
    "ageMin": 18,
    "ageMax": 120,
    "sex": "any",
    "pregnancySensitive": true,
    "optimalMin": 0.5,
    "optimalMax": 2.5,
    "subOptimalBelowMin": 0.3,
    "subOptimalAboveMax": 4.5,
    "actionBelow": 0.3,
    "actionAbove": 4.5,
    "explainer": "TSH is the primary screening test for thyroid function. High TSH typically indicates underactive thyroid; low TSH indicates overactive thyroid. Pregnancy-specific ranges differ significantly — this marker is intentionally not classified when pregnancy status is positive or unknown.",
    "source": "Endocrine Society / AACE Thyroid Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review",
    "reviewedAt": null,
    "isActive": true
  },
  {
    "canonicalId": "hba1c",
    "displayName": "HbA1c",
    "aliases": [
      "Glycated Haemoglobin",
      "Hemoglobin A1c",
      "Glycosylated Haemoglobin",
      "A1C"
    ],
    "category": "Metabolic",
    "canonicalUnit": "%",
    "ageMin": 18,
    "ageMax": 120,
    "sex": "any",
    "pregnancySensitive": true,
    "optimalMin": 4.0,
    "optimalMax": 5.6,
    "subOptimalBelowMin": 3.5,
    "subOptimalAboveMax": 6.4,
    "actionBelow": 3.5,
    "actionAbove": 6.4,
    "explainer": "HbA1c estimates average blood sugar over the prior 2–3 months. 5.7–6.4% is prediabetic range; 6.5% or higher meets diabetes threshold. Pregnancy-specific ranges differ.",
    "source": "American Diabetes Association Standards of Care (2024)",
    "clinicalReviewer": "DRAFT — pending review",
    "reviewedAt": null,
    "isActive": true
  },
  {
    "canonicalId": "hemoglobin",
    "displayName": "Hemoglobin",
    "aliases": ["Hb", "Haemoglobin", "HGB"],
    "category": "CBC",
    "canonicalUnit": "g/dL",
    "ageMin": 18,
    "ageMax": 120,
    "sex": "male",
    "pregnancySensitive": false,
    "optimalMin": 13.5,
    "optimalMax": 17.5,
    "subOptimalBelowMin": 12.5,
    "subOptimalAboveMax": 18.5,
    "actionBelow": 12.5,
    "actionAbove": 18.5,
    "explainer": "Hemoglobin carries oxygen from the lungs to the rest of the body. Low levels indicate anaemia.",
    "source": "WHO anaemia thresholds (2024) + AACC reference intervals",
    "clinicalReviewer": "DRAFT — pending review",
    "reviewedAt": null,
    "isActive": true
  }
]
```

- [ ] **Step 3: Commit the first tranche**

```bash
git add packages/core/seeds/biomarker-ranges.json packages/core/seeds/README.md
git commit -m "feat(phase-2.5a/seed): first 8 biomarker ranges (nutrient + thyroid + metabolic + CBC start)"
```

- [ ] **Step 4: Append tranche 2 — 9 rows (hemoglobin female, Free T3, Free T4, Total Test male/female, Free Test male/female, SHBG male/female)**

Open `packages/core/seeds/biomarker-ranges.json` and append these rows before the closing `]`:

```json
  ,{
    "canonicalId": "hemoglobin",
    "displayName": "Hemoglobin",
    "aliases": ["Hb", "Haemoglobin", "HGB"],
    "category": "CBC",
    "canonicalUnit": "g/dL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": false,
    "optimalMin": 12.0, "optimalMax": 15.5,
    "subOptimalBelowMin": 11.0, "subOptimalAboveMax": 16.5,
    "actionBelow": 11.0, "actionAbove": 16.5,
    "explainer": "Hemoglobin carries oxygen from the lungs to the rest of the body. Low levels indicate anaemia.",
    "source": "WHO anaemia thresholds (2024) + AACC reference intervals",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true
  },
  {
    "canonicalId": "free_t3", "displayName": "Free T3",
    "aliases": ["FT3", "Free Tri-iodothyronine"],
    "category": "Thyroid", "canonicalUnit": "pg/mL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": true,
    "optimalMin": 2.3, "optimalMax": 4.2,
    "subOptimalBelowMin": 2.0, "subOptimalAboveMax": 4.6,
    "actionBelow": 2.0, "actionAbove": 4.6,
    "explainer": "Free T3 is the active thyroid hormone. Low levels often accompany hypothyroidism; high levels accompany hyperthyroidism. Pregnancy-specific ranges differ.",
    "source": "Endocrine Society Thyroid Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true
  },
  {
    "canonicalId": "free_t4", "displayName": "Free T4",
    "aliases": ["FT4", "Free Thyroxine"],
    "category": "Thyroid", "canonicalUnit": "ng/dL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": true,
    "optimalMin": 0.9, "optimalMax": 1.7,
    "subOptimalBelowMin": 0.8, "subOptimalAboveMax": 1.8,
    "actionBelow": 0.8, "actionAbove": 1.8,
    "explainer": "Free T4 is the inactive storage form of thyroid hormone. Interpreted alongside TSH and Free T3. Pregnancy-specific ranges differ.",
    "source": "Endocrine Society Thyroid Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true
  },
  {
    "canonicalId": "total_testosterone", "displayName": "Total Testosterone",
    "aliases": ["Testosterone Total", "Total T"],
    "category": "Hormonal Balance", "canonicalUnit": "ng/dL",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": false,
    "optimalMin": 500, "optimalMax": 900,
    "subOptimalBelowMin": 300, "subOptimalAboveMax": 1100,
    "actionBelow": 300, "actionAbove": 1100,
    "explainer": "Total testosterone drives libido, muscle mass, and hair growth in men. Low levels are associated with fatigue, low libido, and metabolic changes.",
    "source": "Endocrine Society — Testosterone Therapy Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true
  },
  {
    "canonicalId": "total_testosterone", "displayName": "Total Testosterone",
    "aliases": ["Testosterone Total", "Total T"],
    "category": "Hormonal Balance", "canonicalUnit": "ng/dL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": false,
    "optimalMin": 15, "optimalMax": 70,
    "subOptimalBelowMin": 8, "subOptimalAboveMax": 80,
    "actionBelow": 8, "actionAbove": 80,
    "explainer": "Testosterone in women supports bone density, libido, and mood. Elevated levels (outside pregnancy) may indicate PCOS.",
    "source": "Endocrine Society — PCOS Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true
  },
  {
    "canonicalId": "free_testosterone", "displayName": "Free Testosterone",
    "aliases": ["Free T", "FT"],
    "category": "Hormonal Balance", "canonicalUnit": "pg/mL",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": false,
    "optimalMin": 9, "optimalMax": 30,
    "subOptimalBelowMin": 5, "subOptimalAboveMax": 35,
    "actionBelow": 5, "actionAbove": 35,
    "explainer": "Free testosterone is the biologically active fraction unbound to proteins. Interpreted alongside total testosterone and SHBG.",
    "source": "Endocrine Society — Testosterone Therapy Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true
  },
  {
    "canonicalId": "free_testosterone", "displayName": "Free Testosterone",
    "aliases": ["Free T", "FT"],
    "category": "Hormonal Balance", "canonicalUnit": "pg/mL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": false,
    "optimalMin": 0.3, "optimalMax": 1.9,
    "subOptimalBelowMin": 0.1, "subOptimalAboveMax": 2.2,
    "actionBelow": 0.1, "actionAbove": 2.2,
    "explainer": "Free testosterone is the biologically active fraction. Elevated levels in women may indicate PCOS.",
    "source": "Endocrine Society — PCOS Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true
  },
  {
    "canonicalId": "shbg", "displayName": "SHBG",
    "aliases": ["Sex Hormone Binding Globulin"],
    "category": "Hormonal Balance", "canonicalUnit": "nmol/L",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": false,
    "optimalMin": 20, "optimalMax": 60,
    "subOptimalBelowMin": 10, "subOptimalAboveMax": 75,
    "actionBelow": 10, "actionAbove": 75,
    "explainer": "SHBG binds sex hormones in blood. Low SHBG is associated with insulin resistance; high SHBG with reduced free testosterone.",
    "source": "AACC reference intervals (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true
  },
  {
    "canonicalId": "shbg", "displayName": "SHBG",
    "aliases": ["Sex Hormone Binding Globulin"],
    "category": "Hormonal Balance", "canonicalUnit": "nmol/L",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": false,
    "optimalMin": 40, "optimalMax": 120,
    "subOptimalBelowMin": 20, "subOptimalAboveMax": 140,
    "actionBelow": 20, "actionAbove": 140,
    "explainer": "SHBG binds sex hormones in blood. Low SHBG in women may signal insulin resistance or PCOS.",
    "source": "Endocrine Society — PCOS Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true
  }
```

Commit:

```bash
git add packages/core/seeds/biomarker-ranges.json
git commit -m "feat(phase-2.5a/seed): append tranche 2 — 9 rows (hem F, T3/T4, Testo, Free T, SHBG)"
```

- [ ] **Step 5: Append tranche 3 — 12 rows (DHEA-S M/F, LH M/F, FSH M/F, prolactin M/F, estradiol M/F, AMH, iron serum M/F)**

Use the tranche-2 shape — same aliases/category/explainer pattern. Include the rows below. Thresholds derived from Endocrine Society / AACC 2024.

Paste-block (append before closing `]`; each row prefixed with `,`):

```json
,
  { "canonicalId": "dhea_s", "displayName": "DHEA-S",
    "aliases": ["Dehydroepiandrosterone Sulfate", "DHEA Sulfate", "DHEAS"],
    "category": "Hormonal Balance", "canonicalUnit": "µg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": false,
    "optimalMin": 160, "optimalMax": 450,
    "subOptimalBelowMin": 80, "subOptimalAboveMax": 560,
    "actionBelow": 80, "actionAbove": 560,
    "explainer": "DHEA-S is a precursor to sex hormones produced by the adrenal glands. Declines with age; low levels are associated with fatigue.",
    "source": "Endocrine Society adrenal guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "dhea_s", "displayName": "DHEA-S",
    "aliases": ["Dehydroepiandrosterone Sulfate", "DHEA Sulfate", "DHEAS"],
    "category": "Hormonal Balance", "canonicalUnit": "µg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": false,
    "optimalMin": 60, "optimalMax": 350,
    "subOptimalBelowMin": 30, "subOptimalAboveMax": 430,
    "actionBelow": 30, "actionAbove": 430,
    "explainer": "DHEA-S in women supports androgen balance. Elevated levels may indicate adrenal origin of androgen excess.",
    "source": "Endocrine Society PCOS Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "lh", "displayName": "Luteinizing Hormone",
    "aliases": ["LH"],
    "category": "Hormonal Balance", "canonicalUnit": "mIU/mL",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": true,
    "optimalMin": 1.7, "optimalMax": 8.6,
    "subOptimalBelowMin": 1.0, "subOptimalAboveMax": 12,
    "actionBelow": 1.0, "actionAbove": 12,
    "explainer": "LH stimulates testosterone production in men. Low levels may indicate pituitary dysfunction.",
    "source": "AACC reference intervals (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "lh", "displayName": "Luteinizing Hormone",
    "aliases": ["LH"],
    "category": "Hormonal Balance", "canonicalUnit": "mIU/mL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": true,
    "optimalMin": 2.0, "optimalMax": 14,
    "subOptimalBelowMin": 1.0, "subOptimalAboveMax": 25,
    "actionBelow": 1.0, "actionAbove": 25,
    "explainer": "LH drives ovulation. Elevated LH:FSH ratio is a common marker in PCOS. Pregnancy ranges differ significantly.",
    "source": "Endocrine Society PCOS Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "fsh", "displayName": "Follicle-Stimulating Hormone",
    "aliases": ["FSH"],
    "category": "Hormonal Balance", "canonicalUnit": "mIU/mL",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": true,
    "optimalMin": 1.5, "optimalMax": 12.4,
    "subOptimalBelowMin": 1.0, "subOptimalAboveMax": 15,
    "actionBelow": 1.0, "actionAbove": 15,
    "explainer": "FSH supports sperm production in men. Elevated FSH may indicate testicular dysfunction.",
    "source": "AACC reference intervals (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "fsh", "displayName": "Follicle-Stimulating Hormone",
    "aliases": ["FSH"],
    "category": "Hormonal Balance", "canonicalUnit": "mIU/mL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": true,
    "optimalMin": 3.0, "optimalMax": 10,
    "subOptimalBelowMin": 1.0, "subOptimalAboveMax": 30,
    "actionBelow": 1.0, "actionAbove": 30,
    "explainer": "FSH drives follicular development. Elevated FSH suggests diminished ovarian reserve or menopause.",
    "source": "Endocrine Society Ovarian Insufficiency Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "prolactin", "displayName": "Prolactin",
    "aliases": ["PRL"],
    "category": "Hormonal Balance", "canonicalUnit": "ng/mL",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": true,
    "optimalMin": 2, "optimalMax": 18,
    "subOptimalBelowMin": 1, "subOptimalAboveMax": 25,
    "actionBelow": 1, "actionAbove": 25,
    "explainer": "Elevated prolactin in men can suppress testosterone production and cause low libido or fertility issues.",
    "source": "Endocrine Society Hyperprolactinaemia Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "prolactin", "displayName": "Prolactin",
    "aliases": ["PRL"],
    "category": "Hormonal Balance", "canonicalUnit": "ng/mL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": true,
    "optimalMin": 3, "optimalMax": 24,
    "subOptimalBelowMin": 1, "subOptimalAboveMax": 30,
    "actionBelow": 1, "actionAbove": 30,
    "explainer": "Elevated prolactin in non-pregnant women can cause menstrual irregularity. Pregnancy-specific ranges are much higher.",
    "source": "Endocrine Society Hyperprolactinaemia Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "estradiol", "displayName": "Estradiol",
    "aliases": ["E2"],
    "category": "Hormonal Balance", "canonicalUnit": "pg/mL",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": true,
    "optimalMin": 10, "optimalMax": 40,
    "subOptimalBelowMin": 5, "subOptimalAboveMax": 55,
    "actionBelow": 5, "actionAbove": 55,
    "explainer": "Estradiol in men is normal in small quantities. Elevated levels may indicate aromatase excess.",
    "source": "AACC reference intervals (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "estradiol", "displayName": "Estradiol",
    "aliases": ["E2"],
    "category": "Hormonal Balance", "canonicalUnit": "pg/mL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": true,
    "optimalMin": 30, "optimalMax": 400,
    "subOptimalBelowMin": 15, "subOptimalAboveMax": 450,
    "actionBelow": 15, "actionAbove": 450,
    "explainer": "Estradiol varies dramatically across menstrual cycle phases and life stages. Not classified when pregnancy status is unknown or positive.",
    "source": "Endocrine Society menstrual-cycle reference (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "amh", "displayName": "Anti-Müllerian Hormone",
    "aliases": ["AMH"],
    "category": "Hormonal Balance", "canonicalUnit": "ng/mL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": false,
    "optimalMin": 1.0, "optimalMax": 6.8,
    "subOptimalBelowMin": 0.3, "subOptimalAboveMax": 10,
    "actionBelow": 0.3, "actionAbove": 10,
    "explainer": "AMH reflects ovarian reserve. High levels can indicate PCOS; very low levels suggest diminished reserve.",
    "source": "ASRM + Endocrine Society PCOS Guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "iron_serum", "displayName": "Iron (serum)",
    "aliases": ["Serum Iron", "Fe"],
    "category": "Nutrient Health", "canonicalUnit": "µg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": true,
    "optimalMin": 70, "optimalMax": 175,
    "subOptimalBelowMin": 50, "subOptimalAboveMax": 200,
    "actionBelow": 50, "actionAbove": 200,
    "explainer": "Serum iron reflects circulating iron. Interpreted with ferritin and transferrin saturation.",
    "source": "AACC iron panel reference (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "iron_serum", "displayName": "Iron (serum)",
    "aliases": ["Serum Iron", "Fe"],
    "category": "Nutrient Health", "canonicalUnit": "µg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": true,
    "optimalMin": 60, "optimalMax": 160,
    "subOptimalBelowMin": 40, "subOptimalAboveMax": 180,
    "actionBelow": 40, "actionAbove": 180,
    "explainer": "Serum iron reflects circulating iron. Pregnancy-specific ranges differ significantly.",
    "source": "AACC iron panel reference (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true }
```

Commit:

```bash
git add packages/core/seeds/biomarker-ranges.json
git commit -m "feat(phase-2.5a/seed): append tranche 3 — 13 rows (DHEA-S, LH/FSH, prolactin, estradiol, AMH, iron)"
```

- [ ] **Step 6: Append tranche 4 — final 15 rows (fasting glucose, insulin, 4 lipids, ALT, AST, creatinine, uric acid, WBC, platelets, MCV)**

Paste-block (append before closing `]`, each prefixed with `,`):

```json
,
  { "canonicalId": "fasting_glucose", "displayName": "Fasting Glucose",
    "aliases": ["Glucose Fasting", "FBS", "Fasting Blood Sugar"],
    "category": "Metabolic", "canonicalUnit": "mg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": true,
    "optimalMin": 70, "optimalMax": 99,
    "subOptimalBelowMin": 60, "subOptimalAboveMax": 125,
    "actionBelow": 60, "actionAbove": 125,
    "explainer": "Fasting glucose 100–125 mg/dL is prediabetic range; 126+ meets diabetes threshold. Pregnancy thresholds differ.",
    "source": "ADA Standards of Care (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "fasting_insulin", "displayName": "Fasting Insulin",
    "aliases": ["Insulin Fasting"],
    "category": "Metabolic", "canonicalUnit": "µIU/mL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 2, "optimalMax": 8,
    "subOptimalBelowMin": 1, "subOptimalAboveMax": 15,
    "actionBelow": 1, "actionAbove": 15,
    "explainer": "Fasting insulin reflects baseline insulin production. Elevated levels suggest insulin resistance, often before glucose becomes abnormal.",
    "source": "Endocrine Society insulin-resistance review (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "total_cholesterol", "displayName": "Total Cholesterol",
    "aliases": ["TC", "Cholesterol"],
    "category": "Lipids", "canonicalUnit": "mg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 150, "optimalMax": 199,
    "subOptimalBelowMin": 100, "subOptimalAboveMax": 239,
    "actionBelow": 100, "actionAbove": 239,
    "explainer": "Total cholesterol is the sum of LDL, HDL, and other lipoproteins. Interpretation requires the full lipid panel.",
    "source": "ADA/ACC lipid guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "hdl_cholesterol", "displayName": "HDL Cholesterol",
    "aliases": ["HDL", "HDL-C", "Good Cholesterol"],
    "category": "Lipids", "canonicalUnit": "mg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": false,
    "optimalMin": 40, "optimalMax": 85,
    "subOptimalBelowMin": 30, "subOptimalAboveMax": 100,
    "actionBelow": 30, "actionAbove": 100,
    "explainer": "HDL transports cholesterol away from arteries. Higher is protective.",
    "source": "ADA/ACC lipid guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "hdl_cholesterol", "displayName": "HDL Cholesterol",
    "aliases": ["HDL", "HDL-C", "Good Cholesterol"],
    "category": "Lipids", "canonicalUnit": "mg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": false,
    "optimalMin": 50, "optimalMax": 95,
    "subOptimalBelowMin": 35, "subOptimalAboveMax": 110,
    "actionBelow": 35, "actionAbove": 110,
    "explainer": "HDL transports cholesterol away from arteries. Higher is protective.",
    "source": "ADA/ACC lipid guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "ldl_cholesterol", "displayName": "LDL Cholesterol",
    "aliases": ["LDL", "LDL-C", "Bad Cholesterol"],
    "category": "Lipids", "canonicalUnit": "mg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 50, "optimalMax": 99,
    "subOptimalBelowMin": 30, "subOptimalAboveMax": 159,
    "actionBelow": 30, "actionAbove": 159,
    "explainer": "LDL deposits cholesterol in arteries. High LDL is the primary lipid target for cardiovascular risk reduction.",
    "source": "ADA/ACC lipid guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "triglycerides", "displayName": "Triglycerides",
    "aliases": ["TG", "Trigs"],
    "category": "Lipids", "canonicalUnit": "mg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 40, "optimalMax": 149,
    "subOptimalBelowMin": 20, "subOptimalAboveMax": 199,
    "actionBelow": 20, "actionAbove": 199,
    "explainer": "Triglycerides rise after meals and with carbohydrate-heavy diets. Sustained high levels are cardiovascular risk factors.",
    "source": "ADA/ACC lipid guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "alt_sgpt", "displayName": "ALT (SGPT)",
    "aliases": ["ALT", "SGPT", "Alanine Transaminase", "Alanine Aminotransferase"],
    "category": "Liver", "canonicalUnit": "U/L",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 7, "optimalMax": 35,
    "subOptimalBelowMin": 5, "subOptimalAboveMax": 55,
    "actionBelow": 5, "actionAbove": 55,
    "explainer": "ALT rises when liver cells are damaged. Elevated levels may indicate fatty liver, medication injury, or viral hepatitis.",
    "source": "AASLD reference intervals (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "ast_sgot", "displayName": "AST (SGOT)",
    "aliases": ["AST", "SGOT", "Aspartate Transaminase", "Aspartate Aminotransferase"],
    "category": "Liver", "canonicalUnit": "U/L",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 8, "optimalMax": 33,
    "subOptimalBelowMin": 5, "subOptimalAboveMax": 55,
    "actionBelow": 5, "actionAbove": 55,
    "explainer": "AST is present in liver, heart, and muscle. Interpreted alongside ALT.",
    "source": "AASLD reference intervals (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "creatinine", "displayName": "Creatinine",
    "aliases": ["Cr", "Serum Creatinine"],
    "category": "Kidney", "canonicalUnit": "mg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "male",
    "pregnancySensitive": false,
    "optimalMin": 0.7, "optimalMax": 1.3,
    "subOptimalBelowMin": 0.5, "subOptimalAboveMax": 1.5,
    "actionBelow": 0.5, "actionAbove": 1.5,
    "explainer": "Creatinine is a kidney-filtered muscle breakdown product. Elevated levels suggest reduced kidney function.",
    "source": "KDIGO kidney guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "creatinine", "displayName": "Creatinine",
    "aliases": ["Cr", "Serum Creatinine"],
    "category": "Kidney", "canonicalUnit": "mg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "female",
    "pregnancySensitive": false,
    "optimalMin": 0.5, "optimalMax": 1.1,
    "subOptimalBelowMin": 0.4, "subOptimalAboveMax": 1.3,
    "actionBelow": 0.4, "actionAbove": 1.3,
    "explainer": "Creatinine reflects kidney filtration. Lower muscle mass in women gives lower reference range.",
    "source": "KDIGO kidney guidelines (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "uric_acid", "displayName": "Uric Acid",
    "aliases": ["Urate", "Serum Uric Acid"],
    "category": "Metabolic", "canonicalUnit": "mg/dL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 3.0, "optimalMax": 6.0,
    "subOptimalBelowMin": 2.0, "subOptimalAboveMax": 7.5,
    "actionBelow": 2.0, "actionAbove": 7.5,
    "explainer": "Uric acid rises with purine-rich diets and metabolic syndrome. Sustained high levels can precipitate gout.",
    "source": "AACC uric acid reference (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "wbc_total", "displayName": "Total Leukocyte Count",
    "aliases": ["WBC", "TLC", "White Blood Cells"],
    "category": "CBC", "canonicalUnit": "10^3/µL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 4.0, "optimalMax": 10.0,
    "subOptimalBelowMin": 3.0, "subOptimalAboveMax": 12.0,
    "actionBelow": 3.0, "actionAbove": 12.0,
    "explainer": "WBC count reflects immune activity. Elevated counts suggest infection or inflammation; very low counts warrant attention.",
    "source": "AACC CBC reference (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "platelets", "displayName": "Platelet Count",
    "aliases": ["PLT", "Platelets"],
    "category": "CBC", "canonicalUnit": "10^3/µL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 150, "optimalMax": 400,
    "subOptimalBelowMin": 100, "subOptimalAboveMax": 500,
    "actionBelow": 100, "actionAbove": 500,
    "explainer": "Platelets are essential for blood clotting. Low counts can cause bleeding risk; very high counts increase clotting risk.",
    "source": "AACC CBC reference (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true },
  { "canonicalId": "mcv", "displayName": "MCV",
    "aliases": ["Mean Corpuscular Volume"],
    "category": "CBC", "canonicalUnit": "fL",
    "ageMin": 18, "ageMax": 120, "sex": "any",
    "pregnancySensitive": false,
    "optimalMin": 80, "optimalMax": 100,
    "subOptimalBelowMin": 70, "subOptimalAboveMax": 105,
    "actionBelow": 70, "actionAbove": 105,
    "explainer": "MCV is the average red blood cell size. Helps classify anaemia: low MCV suggests iron deficiency; high MCV suggests B12/folate deficiency.",
    "source": "AACC CBC reference (2024)",
    "clinicalReviewer": "DRAFT — pending review", "reviewedAt": null, "isActive": true }
```

Commit:

```bash
git add packages/core/seeds/biomarker-ranges.json
git commit -m "feat(phase-2.5a/seed): append tranche 4 — remaining 15 rows (metabolic, lipids, liver, kidney, CBC)"
```

- [ ] **Step 7: Verify JSON is valid**

Run: `node -e "JSON.parse(require('fs').readFileSync('packages/core/seeds/biomarker-ranges.json', 'utf8'))"`
Expected: no output (parse success). Also open in an editor and visually confirm `[` at start, `]` at end, no trailing commas.

Count rows: `node -e "console.log(JSON.parse(require('fs').readFileSync('packages/core/seeds/biomarker-ranges.json', 'utf8')).length)"`
Expected: **45**.

---

## Task 10: `packages/core/seeds/unit-conversions.json`

**Files:**

- Create: `packages/core/seeds/unit-conversions.json`

- [ ] **Step 1: Write the file**

```json
[
  {
    "canonicalId": "fasting_glucose",
    "rawUnitPattern": "mmol/L",
    "canonicalUnit": "mg/dL",
    "factor": 18.0182
  },
  {
    "canonicalId": "total_cholesterol",
    "rawUnitPattern": "mmol/L",
    "canonicalUnit": "mg/dL",
    "factor": 38.67
  },
  {
    "canonicalId": "hdl_cholesterol",
    "rawUnitPattern": "mmol/L",
    "canonicalUnit": "mg/dL",
    "factor": 38.67
  },
  {
    "canonicalId": "ldl_cholesterol",
    "rawUnitPattern": "mmol/L",
    "canonicalUnit": "mg/dL",
    "factor": 38.67
  },
  {
    "canonicalId": "triglycerides",
    "rawUnitPattern": "mmol/L",
    "canonicalUnit": "mg/dL",
    "factor": 88.57
  },
  {
    "canonicalId": "hemoglobin",
    "rawUnitPattern": "g/L",
    "canonicalUnit": "g/dL",
    "factor": 0.1
  },
  {
    "canonicalId": "total_testosterone",
    "rawUnitPattern": "nmol/L",
    "canonicalUnit": "ng/dL",
    "factor": 28.84
  },
  {
    "canonicalId": "free_testosterone",
    "rawUnitPattern": "pmol/L",
    "canonicalUnit": "pg/mL",
    "factor": 0.288
  },
  {
    "canonicalId": "estradiol",
    "rawUnitPattern": "pmol/L",
    "canonicalUnit": "pg/mL",
    "factor": 0.272
  },
  {
    "canonicalId": "tsh",
    "rawUnitPattern": "mIU/L",
    "canonicalUnit": "µIU/mL",
    "factor": 1
  },
  {
    "canonicalId": "prolactin",
    "rawUnitPattern": "µg/L",
    "canonicalUnit": "ng/mL",
    "factor": 1
  },
  {
    "canonicalId": "ferritin",
    "rawUnitPattern": "µg/L",
    "canonicalUnit": "ng/mL",
    "factor": 1
  },
  {
    "canonicalId": "vitamin_d",
    "rawUnitPattern": "nmol/L",
    "canonicalUnit": "ng/mL",
    "factor": 0.4
  },
  {
    "canonicalId": "vitamin_b12",
    "rawUnitPattern": "pmol/L",
    "canonicalUnit": "pg/mL",
    "factor": 1.355
  },
  {
    "canonicalId": "creatinine",
    "rawUnitPattern": "µmol/L",
    "canonicalUnit": "mg/dL",
    "factor": 0.0113
  }
]
```

- [ ] **Step 2: Validate JSON**

Run: `node -e "const a = JSON.parse(require('fs').readFileSync('packages/core/seeds/unit-conversions.json', 'utf8')); console.log('rows:', a.length)"`
Expected: `rows: 15`.

- [ ] **Step 3: Commit**

```bash
git add packages/core/seeds/unit-conversions.json
git commit -m "feat(phase-2.5a/seed): add unit-conversions seed (15 rows)"
```

---

## Task 11: `convex/biomarker/internal/seedRanges.ts` — internal mutation called by the seeder script

**Files:**

- Create: `convex/biomarker/internal/seedRanges.ts`

- [ ] **Step 1: Write the internal mutation**

```ts
// convex/biomarker/internal/seedRanges.ts
import { v } from "convex/values";

import { internalMutation } from "../../_generated/server";

const rowValidator = v.object({
  canonicalId: v.string(),
  displayName: v.string(),
  aliases: v.array(v.string()),
  category: v.string(),
  canonicalUnit: v.string(),
  ageMin: v.number(),
  ageMax: v.number(),
  sex: v.union(v.literal("male"), v.literal("female"), v.literal("any")),
  pregnancySensitive: v.boolean(),
  optimalMin: v.number(),
  optimalMax: v.number(),
  subOptimalBelowMin: v.optional(v.number()),
  subOptimalAboveMax: v.optional(v.number()),
  actionBelow: v.optional(v.number()),
  actionAbove: v.optional(v.number()),
  explainer: v.string(),
  source: v.string(),
  clinicalReviewer: v.optional(v.string()),
  reviewedAt: v.optional(v.number()),
  isActive: v.boolean(),
});

export const upsertRanges = internalMutation({
  args: { rows: v.array(rowValidator) },
  handler: async (ctx, { rows }) => {
    let inserted = 0;
    let updated = 0;
    const now = Date.now();

    for (const row of rows) {
      const existing = await ctx.db
        .query("biomarker_reference_ranges")
        .withIndex("by_canonical_id", (q) =>
          q
            .eq("canonicalId", row.canonicalId)
            .eq("sex", row.sex)
            .eq("ageMin", row.ageMin),
        )
        .filter((q) => q.eq(q.field("ageMax"), row.ageMax))
        .unique();

      if (existing) {
        await ctx.db.patch(existing._id, { ...row, updatedAt: now });
        updated += 1;
      } else {
        await ctx.db.insert("biomarker_reference_ranges", {
          ...row,
          updatedAt: now,
        });
        inserted += 1;
      }
    }

    return { inserted, updated, total: rows.length };
  },
});
```

- [ ] **Step 2: Typecheck**

Run: `pnpm -w typecheck`
Expected: green.

- [ ] **Step 3: Commit**

```bash
git add convex/biomarker/internal/seedRanges.ts
git commit -m "feat(phase-2.5a/seed): internal mutation upsertRanges"
```

---

## Task 12: `scripts/seed-biomarker-ranges.ts` — Node seeder script

**Files:**

- Create: `scripts/seed-biomarker-ranges.ts`

- [ ] **Step 1: Write the script**

```ts
// scripts/seed-biomarker-ranges.ts
//
// Reads packages/core/seeds/biomarker-ranges.json, validates each row
// (shape + threshold ordering + required fields), then invokes
// convex/biomarker/internal/seedRanges:upsertRanges via the Convex CLI.
//
// Guards:
//   - Threshold ordering invariant (fails on any violation).
//   - `pregnancySensitive` MUST be an explicit boolean on every row.
//   - `source` MUST be a non-empty string.
//   - `clinicalReviewer` of "" or "DRAFT — pending review" is rejected unless
//     ALLOW_UNREVIEWED_RANGES=1.
//   - Prod deployment ALWAYS blocks unreviewed rows regardless of env flag.
//
// Usage:
//   pnpm seed:biomarker-ranges
//   ALLOW_UNREVIEWED_RANGES=1 pnpm seed:biomarker-ranges   (dev only)

import { ConvexHttpClient } from "convex/browser";
import { readFileSync } from "node:fs";
import { join, resolve } from "node:path";
import { z } from "zod";

import { internal } from "../convex/_generated/api";

const rowSchema = z
  .object({
    canonicalId: z.string().min(1),
    displayName: z.string().min(1),
    aliases: z.array(z.string()),
    category: z.string().min(1),
    canonicalUnit: z.string().min(1),
    ageMin: z.number().int().min(0),
    ageMax: z.number().int().min(0),
    sex: z.enum(["male", "female", "any"]),
    pregnancySensitive: z.boolean(),
    optimalMin: z.number(),
    optimalMax: z.number(),
    subOptimalBelowMin: z.number().optional(),
    subOptimalAboveMax: z.number().optional(),
    actionBelow: z.number().optional(),
    actionAbove: z.number().optional(),
    explainer: z.string().min(1),
    source: z.string().min(1),
    clinicalReviewer: z.string().optional(),
    reviewedAt: z.number().nullable().optional(),
    isActive: z.boolean(),
  })
  .superRefine((row, ctx) => {
    if (row.ageMin >= row.ageMax) {
      ctx.addIssue({
        code: "custom",
        message: `ageMin (${row.ageMin}) must be < ageMax (${row.ageMax})`,
      });
    }
    if (row.optimalMin > row.optimalMax) {
      ctx.addIssue({
        code: "custom",
        message: `optimalMin (${row.optimalMin}) must be <= optimalMax (${row.optimalMax})`,
      });
    }
    if (
      row.subOptimalBelowMin !== undefined &&
      row.subOptimalBelowMin >= row.optimalMin
    ) {
      ctx.addIssue({
        code: "custom",
        message: `subOptimalBelowMin (${row.subOptimalBelowMin}) must be < optimalMin (${row.optimalMin})`,
      });
    }
    if (
      row.subOptimalAboveMax !== undefined &&
      row.subOptimalAboveMax <= row.optimalMax
    ) {
      ctx.addIssue({
        code: "custom",
        message: `subOptimalAboveMax (${row.subOptimalAboveMax}) must be > optimalMax (${row.optimalMax})`,
      });
    }
    if (row.actionBelow !== undefined && row.subOptimalBelowMin !== undefined) {
      if (row.actionBelow > row.subOptimalBelowMin) {
        ctx.addIssue({
          code: "custom",
          message: `actionBelow (${row.actionBelow}) must be <= subOptimalBelowMin (${row.subOptimalBelowMin})`,
        });
      }
    }
    if (row.actionAbove !== undefined && row.subOptimalAboveMax !== undefined) {
      if (row.actionAbove < row.subOptimalAboveMax) {
        ctx.addIssue({
          code: "custom",
          message: `actionAbove (${row.actionAbove}) must be >= subOptimalAboveMax (${row.subOptimalAboveMax})`,
        });
      }
    }
  });

const PROD_PATTERNS = [/^prod$/i, /prod$/i, /^production$/i];

function isUnreviewed(row: z.infer<typeof rowSchema>): boolean {
  const r = row.clinicalReviewer;
  return !r || r === "" || r === "DRAFT — pending review";
}

async function main() {
  const seedPath = resolve(
    process.cwd(),
    "packages/core/seeds/biomarker-ranges.json",
  );
  const raw = readFileSync(seedPath, "utf8");
  const parsed = JSON.parse(raw);

  if (!Array.isArray(parsed)) {
    throw new Error("seed file must be a top-level JSON array");
  }

  const rows = parsed.map((row, i) => {
    const result = rowSchema.safeParse(row);
    if (!result.success) {
      console.error(
        `Row ${i} (${row.canonicalId}/${row.sex}) failed validation:`,
      );
      for (const issue of result.error.issues) {
        console.error(`  - ${issue.path.join(".")}: ${issue.message}`);
      }
      process.exit(1);
    }
    return result.data;
  });

  const deployment = process.env.CONVEX_DEPLOYMENT ?? "";
  const isProd = PROD_PATTERNS.some((p) => p.test(deployment));
  const allowUnreviewed = process.env.ALLOW_UNREVIEWED_RANGES === "1";

  const unreviewed = rows.filter(isUnreviewed);
  if (unreviewed.length > 0) {
    if (isProd) {
      console.error(
        `\n⛔ PROD DEPLOYMENT BLOCKED: ${unreviewed.length} rows are unreviewed.`,
      );
      console.error(
        "   Every row in biomarker-ranges.json must have a real `clinicalReviewer`",
      );
      console.error(
        "   set by a named clinical advisor before deploying to production.",
      );
      process.exit(1);
    }
    if (!allowUnreviewed) {
      console.error(
        `\n⛔ ${unreviewed.length} unreviewed rows. Re-run with ALLOW_UNREVIEWED_RANGES=1 (dev only).`,
      );
      process.exit(1);
    }
    console.warn(
      `\n⚠  Loading ${unreviewed.length} unreviewed DRAFT rows (dev override).`,
    );
  }

  const convexUrl = process.env.CONVEX_URL;
  if (!convexUrl) {
    throw new Error(
      "CONVEX_URL env var is required (read from .env.local or Convex dashboard)",
    );
  }

  const client = new ConvexHttpClient(convexUrl);
  const result = await client.mutation(
    internal.biomarker.internal.seedRanges.upsertRanges,
    { rows },
  );

  console.log(
    `✓ seeded: inserted=${result.inserted} updated=${result.updated} total=${result.total}`,
  );
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

- [ ] **Step 2: Add `seed:biomarker-ranges` script to root `package.json`**

Edit root `package.json`, add to scripts:

```json
"seed:biomarker-ranges": "tsx scripts/seed-biomarker-ranges.ts"
```

- [ ] **Step 3: Verify `tsx` is installed (or install)**

Run: `pnpm -w ls tsx --depth 0`
If absent: `pnpm -w add -D tsx`

- [ ] **Step 4: Run the seeder against the dev Convex deployment**

Ensure `.env.local` has `CONVEX_URL=<dev url>` (copy from the Convex dashboard's HTTP URL).

Run:

```bash
CONVEX_DEPLOYMENT=$(cat .env.local | grep CONVEX_DEPLOYMENT | cut -d= -f2) \
  ALLOW_UNREVIEWED_RANGES=1 \
  pnpm seed:biomarker-ranges
```

Expected: `✓ seeded: inserted=45 updated=0 total=45`.

Re-run immediately. Expected: `✓ seeded: inserted=0 updated=45 total=45` (idempotent).

- [ ] **Step 5: Commit**

```bash
git add scripts/seed-biomarker-ranges.ts package.json pnpm-lock.yaml
git commit -m "feat(phase-2.5a/seed): seeder script with validation + prod/unreviewed guards"
```

---

## Task 13: `convex/__tests__/biomarker/seed-validation.test.ts` — CI guard

**Files:**

- Create: `convex/__tests__/biomarker/seed-validation.test.ts`

- [ ] **Step 1: Write the test**

```ts
// convex/__tests__/biomarker/seed-validation.test.ts
//
// CI guard for Phase 2.5 biomarker seed data. Runs in `pnpm test:seed`
// BEFORE any deploy. Validates structure + threshold ordering on every
// row in packages/core/seeds/biomarker-ranges.json.

import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import { describe, expect, it } from "vitest";
import { z } from "zod";

const rowSchema = z.object({
  canonicalId: z.string().min(1),
  displayName: z.string().min(1),
  aliases: z.array(z.string()),
  category: z.string().min(1),
  canonicalUnit: z.string().min(1),
  ageMin: z.number().int().min(0),
  ageMax: z.number().int().min(0),
  sex: z.enum(["male", "female", "any"]),
  pregnancySensitive: z.boolean(),
  optimalMin: z.number(),
  optimalMax: z.number(),
  subOptimalBelowMin: z.number().optional(),
  subOptimalAboveMax: z.number().optional(),
  actionBelow: z.number().optional(),
  actionAbove: z.number().optional(),
  explainer: z.string().min(20),
  source: z.string().min(5),
  clinicalReviewer: z.string().optional(),
  reviewedAt: z.number().nullable().optional(),
  isActive: z.boolean(),
});

function loadRows() {
  const path = resolve(
    process.cwd(),
    "packages/core/seeds/biomarker-ranges.json",
  );
  return JSON.parse(readFileSync(path, "utf8")) as unknown[];
}

describe("biomarker-ranges.json", () => {
  const rows = loadRows();

  it("is a non-empty array", () => {
    expect(Array.isArray(rows)).toBe(true);
    expect(rows.length).toBeGreaterThanOrEqual(45);
  });

  it("every row matches the schema", () => {
    for (const [i, row] of rows.entries()) {
      const result = rowSchema.safeParse(row);
      if (!result.success) {
        throw new Error(
          `Row ${i} failed: ${result.error.issues.map((x) => x.message).join("; ")}`,
        );
      }
    }
  });

  it("every row has ageMin < ageMax", () => {
    for (const row of rows as z.infer<typeof rowSchema>[]) {
      expect(row.ageMin).toBeLessThan(row.ageMax);
    }
  });

  it("optimal band is well-ordered (min <= max)", () => {
    for (const row of rows as z.infer<typeof rowSchema>[]) {
      expect(row.optimalMin).toBeLessThanOrEqual(row.optimalMax);
    }
  });

  it("subOptimalBelowMin < optimalMin (when set)", () => {
    for (const row of rows as z.infer<typeof rowSchema>[]) {
      if (row.subOptimalBelowMin !== undefined) {
        expect(row.subOptimalBelowMin).toBeLessThan(row.optimalMin);
      }
    }
  });

  it("subOptimalAboveMax > optimalMax (when set)", () => {
    for (const row of rows as z.infer<typeof rowSchema>[]) {
      if (row.subOptimalAboveMax !== undefined) {
        expect(row.subOptimalAboveMax).toBeGreaterThan(row.optimalMax);
      }
    }
  });

  it("actionBelow <= subOptimalBelowMin (when both set)", () => {
    for (const row of rows as z.infer<typeof rowSchema>[]) {
      if (
        row.actionBelow !== undefined &&
        row.subOptimalBelowMin !== undefined
      ) {
        expect(row.actionBelow).toBeLessThanOrEqual(row.subOptimalBelowMin);
      }
    }
  });

  it("actionAbove >= subOptimalAboveMax (when both set)", () => {
    for (const row of rows as z.infer<typeof rowSchema>[]) {
      if (
        row.actionAbove !== undefined &&
        row.subOptimalAboveMax !== undefined
      ) {
        expect(row.actionAbove).toBeGreaterThanOrEqual(row.subOptimalAboveMax);
      }
    }
  });

  it("pregnancySensitive is explicitly boolean on every row", () => {
    for (const row of rows as z.infer<typeof rowSchema>[]) {
      expect(typeof row.pregnancySensitive).toBe("boolean");
    }
  });

  it("source is a non-trivial citation", () => {
    for (const row of rows as z.infer<typeof rowSchema>[]) {
      expect(row.source.length).toBeGreaterThan(4);
    }
  });

  it("sex-differentiated markers have both male and female rows OR a 'any' row", () => {
    const byCanonical = new Map<string, z.infer<typeof rowSchema>[]>();
    for (const row of rows as z.infer<typeof rowSchema>[]) {
      if (!byCanonical.has(row.canonicalId)) {
        byCanonical.set(row.canonicalId, []);
      }
      byCanonical.get(row.canonicalId)!.push(row);
    }
    for (const [id, group] of byCanonical) {
      const hasAny = group.some((r) => r.sex === "any");
      const hasMale = group.some((r) => r.sex === "male");
      const hasFemale = group.some((r) => r.sex === "female");
      expect(hasAny || (hasMale && hasFemale) || hasMale || hasFemale).toBe(
        true,
      );
      // at least one row exists for the canonicalId — coverage check
      expect(group.length).toBeGreaterThan(0);
    }
  });

  it("all 32 canonical markers from the spec are represented", () => {
    const expected = [
      "vitamin_d",
      "vitamin_b12",
      "folate_b9",
      "ferritin",
      "iron_serum",
      "tsh",
      "free_t3",
      "free_t4",
      "total_testosterone",
      "free_testosterone",
      "shbg",
      "dhea_s",
      "lh",
      "fsh",
      "prolactin",
      "estradiol",
      "amh",
      "hba1c",
      "fasting_glucose",
      "fasting_insulin",
      "total_cholesterol",
      "hdl_cholesterol",
      "ldl_cholesterol",
      "triglycerides",
      "alt_sgpt",
      "ast_sgot",
      "creatinine",
      "uric_acid",
      "hemoglobin",
      "wbc_total",
      "platelets",
      "mcv",
    ];
    const present = new Set(
      (rows as z.infer<typeof rowSchema>[]).map((r) => r.canonicalId),
    );
    for (const id of expected) {
      expect(present.has(id)).toBe(true);
    }
    expect(present.size).toBe(expected.length);
  });

  it("pregnancy-sensitive marker set matches the clinical decision", () => {
    const expectedSensitive = new Set([
      "tsh",
      "free_t3",
      "free_t4",
      "ferritin",
      "iron_serum",
      "hemoglobin",
      "hba1c",
      "fasting_glucose",
      "estradiol",
      "lh",
      "fsh",
      "prolactin",
    ]);
    for (const row of rows as z.infer<typeof rowSchema>[]) {
      if (expectedSensitive.has(row.canonicalId)) {
        expect(row.pregnancySensitive).toBe(true);
      } else {
        expect(row.pregnancySensitive).toBe(false);
      }
    }
  });
});
```

- [ ] **Step 2: Run the test**

Run: `pnpm vitest run convex/__tests__/biomarker/seed-validation.test.ts`
Expected: all tests pass.

If a test fails, the seed data is wrong — fix the row(s) in `biomarker-ranges.json` until green. Do not weaken the test.

- [ ] **Step 3: Commit**

```bash
git add convex/__tests__/biomarker/seed-validation.test.ts
git commit -m "test(phase-2.5a/seed): CI guard on threshold ordering + schema + 32-marker coverage"
```

---

## Task 14: Wire `pnpm test:seed` + CI integration

**Files:**

- Modify: root `package.json`
- Modify: `turbo.json` (if test tasks are declared there)

- [ ] **Step 1: Add `test:seed` script to root `package.json`**

In the `scripts` object:

```json
"test:seed": "vitest run convex/__tests__/biomarker/seed-validation.test.ts"
```

And update the aggregate `test` script to include it. Read the existing `"test"` entry and append ` && pnpm test:seed` if it doesn't already cover vitest on this path.

- [ ] **Step 2: Verify the script works**

Run: `pnpm test:seed`
Expected: same green as Task 13 Step 2.

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "ci(phase-2.5a): wire pnpm test:seed into aggregate test pipeline"
```

---

## Task 15: Full-suite sanity check

- [ ] **Step 1: Typecheck all workspaces**

Run: `pnpm -w typecheck`
Expected: green across all 7 workspaces.

- [ ] **Step 2: Lint all workspaces**

Run: `pnpm -w lint`
Expected: green. Mobile may emit pre-existing `MODULE_TYPELESS_PACKAGE_JSON` warnings from Node — those are fine.

- [ ] **Step 3: Run the full test suite (mobile + convex + seed)**

Run: `pnpm -w test`
Expected: all existing Phase 2C tests still pass (150 mobile, 10 convex) + the new seed-validation tests pass.

- [ ] **Step 4: Verify Convex dev deployment is healthy**

Run: `npx convex dev --once --typecheck-components`
Expected: schema push succeeds; no type errors.

- [ ] **Step 5: Verify seed script still idempotent**

Run: `ALLOW_UNREVIEWED_RANGES=1 pnpm seed:biomarker-ranges`
Expected: `✓ seeded: inserted=0 updated=45 total=45`.

- [ ] **Step 6: Commit (if any files changed as side-effect)**

```bash
git status
# if nothing changed, skip commit
```

---

## Task 16: Founder visual verification on Expo Go iOS

- [ ] **Step 1: Start the mobile dev server**

Run: `pnpm --filter @onlyou/mobile start`
Expected: Metro bundler running; QR code displayed.

- [ ] **Step 2: Open Expo Go on iOS, scan QR**

Navigate from the welcome screen to `/design-biomarker-preview` (type the URL into the Expo Go URL field, or add a temp debug link somewhere).

Verify:

- Warm parchment background (warmer than the existing Clinical Luxe screens).
- "Biomarker Report" heading renders in Instrument Serif — tall, contrast-y, editorial.
- Mono tagline is JetBrains Mono, uppercase, letter-spaced.
- 4 marker rows with correct status color mapping.

- [ ] **Step 3: Founder sign-off on fonts + palette**

Ask the founder to confirm:

- Fonts feel right (serif + mono pairing).
- Warm parchment doesn't clash with anything from memory of Phase 2C screens.
- Status colors read correctly — sage = green-optimal, rose = red-action, lavender = sub-optimal, grey = unclassified.

If the founder wants palette tweaks, adjust `packages/core/src/tokens/biomarker.ts`, commit, re-verify. Do NOT move on until visually approved.

---

## Task 17: Remove the temporary playground screen

**Files:**

- Delete: `apps/mobile/app/design-biomarker-preview.tsx`

- [ ] **Step 1: Delete the file**

```bash
rm apps/mobile/app/design-biomarker-preview.tsx
```

- [ ] **Step 2: Verify the app still boots**

Run: `pnpm --filter @onlyou/mobile start`
Expected: no errors; welcome screen renders.

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/app/design-biomarker-preview.tsx
git commit -m "chore(phase-2.5a): remove temporary biomarker preview screen"
```

---

## Task 18: First-pass code review

- [ ] **Step 1: Invoke requesting-code-review skill**

Use the Skill tool to invoke `superpowers:requesting-code-review` with this context:

```
Reviewing Phase 2.5A — Biomarker Foundation. Scope is non-runtime:
design tokens (editorial palette + fonts), two-register ESLint
guardrails, 8 new Convex tables (schema only, no callers yet), 32-marker
seed JSON (45 rows, all DRAFT pending clinical review), seeder script,
CI seed-validation guard, dev-only admin scaffold, convex/lib/claude.ts
stub with fail-loud placeholders. No parse pipeline, no intake
mutation, no mobile UI beyond a token-preview screen (now removed).

Plan: docs/superpowers/plans/2026-04-18-phase-2.5a-foundation.md
Spec: docs/superpowers/specs/2026-04-17-phase-2.5-biomarker-foundation-design.md
Decisions: docs/decisions/2026-04-17-{biomarker-design-register,
  phase-2.5-scope-boundary, pregnancy-safety-guard,
  claude-vision-native-parsing, biomarker-values-split}.md

Focus the review on:
1. Schema correctness vs spec §2 (every table, every index named correctly).
2. Seed-data invariants vs spec §3.5 (inclusive-inclusive thresholds,
   pregnancy-sensitive list of 12 specific markers).
3. Seeder-script safety (unreviewed guard, prod hard-fail).
4. ESLint rule actually blocks the cross-register imports (both ways).
5. convex/lib/claude.ts stub is fail-loud (throws on invocation, not silent).

Out of scope — do not comment on parse pipeline, UI components,
Claude integration, curation admin, portal contracts; those are
Plans 2.5B/C/D.
```

- [ ] **Step 2: Save the review report**

Review output saved to `docs/superpowers/reviews/2026-04-18-phase-2.5a-foundation-review.md`.

---

## Task 19: Apply review fixes

- [ ] **Step 1: Read the review report**

Run: `cat docs/superpowers/reviews/2026-04-18-phase-2.5a-foundation-review.md`

- [ ] **Step 2: Address each BLOCKER + MAJOR finding**

Each fix = one atomic commit. Commit message: `fix(phase-2.5a/review): <finding-id> — <short description>`. Re-run `pnpm -w test:seed && pnpm -w typecheck && pnpm -w lint` after each fix.

- [ ] **Step 3: Address MINOR + SUGGESTION findings at discretion**

Fix the ones that clearly add value. Defer the rest to DEFERRED with a destination phase named.

- [ ] **Step 4: Invoke second-pass review if any BLOCKER was found**

Use `superpowers:requesting-code-review` again; report saved alongside the first-pass review.

---

## Task 20: Merge prep

**Files:**

- Create: `docs/decisions/2026-04-18-phase-2.5a-as-built.md`
- Modify: `checkpoint.md`

- [ ] **Step 1: Write the as-built retro**

```markdown
---
date: "2026-04-18"
phase: 2.5a
status: complete
tags:
  - retro
  - phase-2.5a
  - as-built
---

# Phase 2.5A — Completion Retro (As-Built)

**Date:** 2026-04-18
**Phase:** 2.5A (Biomarker Foundation — tokens + schema + seed)
**Branch (merged):** `feature/phase-2.5a-foundation`
**Plan:** [[superpowers/plans/2026-04-18-phase-2.5a-foundation]]
**Review:** [[superpowers/reviews/2026-04-18-phase-2.5a-foundation-review]]
**Next:** Plan 2.5B — Parse pipeline.

## What shipped

- 8 new Convex tables + `users.pregnancyStatus` additive field.
- `biomarkerPalette` + `biomarkerFonts` tokens with two-register ESLint guardrail.
- 4 new font TTFs (Instrument Serif + JetBrains Mono × 2 weights).
- `biomarker-ranges.json` seeded with 45 DRAFT rows across 32 canonical markers.
- `unit-conversions.json` seeded with 15 rows.
- `scripts/seed-biomarker-ranges.ts` seeder with zod validation + unreviewed/prod guards.
- `seed-validation.test.ts` CI guard.
- `convex/lib/claude.ts` fail-loud stub with model-ID constants.
- `convex/admin.ts` dev-only scaffold.
- `biomarker_parsing_enabled` feature flag seeded `true`.

## As-built commit count + test counts

- Commits on branch: <count>
- `pnpm test` — all green (150 mobile + 10 convex + N seed-validation)
- `pnpm typecheck` — green across 7 workspaces
- `pnpm lint` — green

## Deferred (to Plans 2.5B/C/D — see DEFERRED.md)

- Parse pipeline, Claude integration → 2.5B
- Intake mutation, notifications, curation admin, portal contracts → 2.5C
- Mobile biomarker components + 4 screens + upload flow → 2.5D
- Clinical advisor sign-off on the 45 DRAFT rows → Phase 2.5 prerequisite before merge to prod
```

- [ ] **Step 2: Update `checkpoint.md`**

Set `Current phase: Phase 2.5A — COMPLETE. Plan 2.5B queued.` with a link to this retro + next plan.

- [ ] **Step 3: Commit docs**

```bash
git add docs/decisions/2026-04-18-phase-2.5a-as-built.md checkpoint.md
git commit -m "docs(phase-2.5a): as-built retro + checkpoint update"
```

- [ ] **Step 4: Merge prep — rebase on master and push**

```bash
git rebase master
git push -u origin feature/phase-2.5a-foundation
```

Open a PR. Title: `phase 2.5a — biomarker foundation (tokens + schema + seed)`. Body: link to spec + plan + review.

- [ ] **Step 5: Merge after PR approval**

```bash
git checkout master
git merge --no-ff feature/phase-2.5a-foundation -m "Merge phase 2.5a — biomarker foundation"
git push origin master
```

Worktree + branch remain; may be removed after Plan 2.5B is well underway.

---

## Self-review checklist

After executing this plan, verify:

- [ ] Every table in spec §2 (lab_reports, biomarker_reports, biomarker_values, biomarker_reference_ranges, biomarker_curation_queue, lab_orders, parse_rate_limits, notifications) is defined in Task 1 with every index named in the spec.
- [ ] `users.pregnancyStatus` added with the three literal values plus implicit null-default.
- [ ] `biomarkerPalette` includes `sage`, `rose`, `lavender`, `grey` (the 4 status colors for the 4 `StatusBadge` variants in Plan 2.5D).
- [ ] ESLint rule fires when `biomarkerPalette` is imported from a non-biomarker path (Task 6 Step 3 probe).
- [ ] Seeder script hard-fails in prod regardless of `ALLOW_UNREVIEWED_RANGES` (Task 12 logic).
- [ ] `seed-validation.test.ts` enforces the exact 32-marker list and the exact 12-marker pregnancy-sensitive set (Task 13).
- [ ] `convex/lib/claude.ts` `extractMarkers` and `generateNarrative` throw on invocation — they do not return silent stubs.
- [ ] Playground screen is deleted before merge.

No placeholders in this plan. Every code block shows the actual content the engineer types.
