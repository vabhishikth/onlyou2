# Decision — Phase 2.5D live E2E (Task 4.4)

**Date:** 2026-04-23
**Branch:** `phase-2.5d` · worktree `D:/onlyou2-phase-2.5d`
**Phase:** 2.5D Wave 4 gate — prove real-data round-trip before Wave 5.

## Goal

Exercise the full patient biomarker pipe end-to-end against a live Convex dev deployment, on a physical iPhone running Expo Go — not unit tests with mocks. Per the live-E2E phase-gate rule (memory: `feedback_live_e2e_phase_gate.md`), every Convex phase runs one real round-trip before it's marked done.

## Setup

Non-technical founder could not reasonably hand-seed `biomarker_reports` + `biomarker_values` via the Convex dashboard Data tab. Seed helper written instead:

- `convex/seed/devBiomarkerReport.ts` — idempotent one-click seeder.
  - `seedArjunReport` (internalAction) — orchestrates: finds Arjun user by phone, finds 5 reference-range IDs by canonicalId, stores a 4-byte fake PDF blob, inserts `lab_reports` → `biomarker_reports` → 5 `biomarker_values` chain.
  - `clearArjunReports` (internalMutation) — tear-down by `[DEV SEED]` narrative marker.
  - Prod guard: `assertNotProd()` on every entry point.
- Picked 5 canonicalIds that mix statuses for visual coverage: `total_cholesterol` (185, optimal), `hdl_cholesterol` (38, sub_optimal), `ldl_cholesterol` (165, action_required), `hemoglobin` (14.2, optimal), `vitamin_d` (22, sub_optimal).

Deploy path: `npx convex dev --once` → `npx convex run seed/devBiomarkerReport:seedArjunReport` returned `{ biomarkerReportId, labReportId, valuesCreated: 5 }`.

Env flip: `apps/mobile/.env.local` appended `EXPO_PUBLIC_USE_MOCK_BIOMARKERS=0` to opt the hook into real Convex data.

## Test execution

Founder on physical iPhone via Expo Go, signed in as Arjun (`+91 99999 00001` / dev-bypass OTP `000000`), scenario switcher banner toggled ON.

### What passed ✅

| Check                                                        | Result                                                                        |
| ------------------------------------------------------------ | ----------------------------------------------------------------------------- |
| Dashboard renders 5 cards (not 24 demo rows)                 | ✅ Yes                                                                        |
| Correct names (Total/HDL/LDL/Hgb/Vit D)                      | ✅ Yes                                                                        |
| Each card shows a value + status colour                      | ✅ Yes                                                                        |
| Detail screen opens when tapping a card                      | ✅ Yes — Dial, Hero value, explainer, trend card, reference card all rendered |
| Deep-link `exp://<host>/--/lab-results` → Dashboard          | ✅ Navigates from home → Dashboard                                            |
| Deep-link `exp://<host>/--/lab-results/<value._id>` → Detail | ✅ Navigates directly to Total Cholesterol detail                             |

**Conclusion:** the data pipe is live. Session-token auth, patient query, canonical join, hook transform, Dashboard render, Detail render, and deep-link handler all work against real Convex data.

### What did not pass (known DEFERRED items surfaced visually)

| Observation                                          | Root cause                                                                                                                                                                                                                                  | Already logged                                              |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| Sparkline on cards renders as a single dot (no line) | Hook sets `trend: [currentValue]` because historical-report join is not yet wired (only one report per patient seeded). Single data point ⇒ single dot.                                                                                     | DEFERRED → Phase 3 (multi-report historical trend query)    |
| Markers on RangeBar clipped at top + right           | Two stacked issues:<br> • glow Circle extends 2px above SVG viewbox → silently clipped (pre-existing `RangeBar` port issue)<br> • placeholder `low=0/high=100` in hook does not match values like 185/165 → marker x > 100% → clipped right | DEFERRED → Wave 5 (viewbox expand) + 2.5E (real range join) |

## Decision

**Task 4.4 verdict: PASS.** The live round-trip goal was met. Visual defects observed are all pre-identified in the DEFERRED ledger and are scheduled. No new work is pulled forward into 4.4.

**Escalation for 2.5E:** founder saw the placeholder-range clipping as a first-impression visual bug. Treat the `biomarker_reference_ranges` join as a **must-ship-first** item for 2.5E before any other patient-visible biomarker work. The current hook placeholder (`low/high/optLow/optHigh = 0/100/25/75`) is visibly broken for real values and should not be demoed to external stakeholders until replaced.

## Commands run (repro)

```bash
# Deploy seed helper to dev deployment
npx convex dev --once

# Seed Arjun with one synthetic report + 5 values
npx convex run seed/devBiomarkerReport:seedArjunReport

# Tear-down when needed
npx convex run seed/devBiomarkerReport:clearArjunReports
```

## Artefacts

- Seed helper: `convex/seed/devBiomarkerReport.ts`
- Seeded IDs (dev deployment only):
  - `biomarkerReportId: jx77ba86fb74jkqxyg70d2w6pn85cpaf`
  - Value IDs: `k17ec1xtm9y8w5sprjfvart7j985d01h` (Total Cholesterol), `k1761dzsmrptq7ht6j7svdskb585d5f5` (HDL), `k17ahy1mapyp2jk01at6zxnv2h85cvrw` (LDL), `k17bk2gjf59htd8mhb3vsvqg5185c9mb` (Hgb), `k1769carwpt8rrf32dd8vadbnx85cqvz` (Vit D)
